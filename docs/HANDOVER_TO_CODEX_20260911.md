# 交接给 Codex:按点值守/唤醒链(2026-09-11 03:5x,DSH 主会话)

> 背景:老板决定"按点值守这条链"改由 Codex 执行(DSH 主会话上下文已很长)。
> 本文件是**交接单**:接手需要的事实、命令、护栏、以及一条必须知道的硬规则。
> DSH 侧交接后只保留:答疑 + 按需执行,**不再自建提醒、不改平台配置**。

## 一、必须先知道的两条硬规则(否则白设)
1. **`schedule_create` 的提醒是会话级的**:到点后以 follow-up 消息投给**创建它的那个会话**。
   所以 **Codex 建的提醒叫醒的是 Codex 自己,不会叫醒 DSH**。要让 DSH 到点醒,只有:
   (a) DSH 会话自建提醒(DSH 上下文已贵,老板不希望);或 (b) 走下面的"叫醒链"。
2. **叫醒 DSH 主会话的唯一现成通道是"看板门铃",且只认老板署名**:
   `tools/mobile_chat/dsh_board_sentinel.py` 的 `WAKE_PATTERN` = 行首
   `- @dsh-老员工|@dsh-main|@dsh <YYYY-MM-DD HH:MM> <作者>：`,并且 `AUTHOR = "老板"`。
   → **任何 Codex 线写的看板行都不会唤醒 DSH**;公告(`@全体`)是按"知悉铃",不要求回复。
   要放开这个限制 = 平台改造(改哨兵署名规则或改走 Web RPC),属老板/Codex 决策。

## 二、当前状态(接手基线)
- **账户**:账1 `a0de4b75`(nav 199,013 / 可用 43,253;持仓 603256 400@126.79、301358 800@55.549、002595 1000@40.82、688775 200@103.69);账2 `5e3d5c21`(20 万现金,空仓)。
- **值守**:引擎 pid 32056 / 托盘 pid 25456(03:13–03:14 起);心跳 `outputs/watch_heartbeat.txt`(交易时段每 20s);时段表 `duty/schedule.yaml`(09:15/09:30/11:30/13:00/14:40/**14:44 执行**/15:00/15:05)。
- **9/11 计划** `outputs/next_plan.csv`:卖 603256 -100@129.10、301358 -200@55.41、002595 -200@40.28;买 **000893 +800@43.22**、**300450 +1100@30.07**;**14:44 引擎自动执行**(无价则不动作)。
- **账2 草案** `outputs/plan2_build_20260911.csv`(8 票 150,302,留 49,698;与账1 无重票);
  执行器 `scripts/build_acct2.py --date 20260911 --batch 1|2`(**默认 dry;`--send` 才放单**);
  纪律:开盘 **先 batch1 --dry 留痕** → **100 股连通测试**确认 `C:\emq\日内分钟` 被东财终端扫单 → `batch1 --send` → **10:30 前 batch2**;不追高(实时价 > 9/10 收盘×1.015 跳过);
  dry 留痕已归档 `outputs/inbox/reports/dsh_20260911_acct2_dry.md`;签字结论见看板 00:30/02:20 两行(AR-002 文件已被还原删除)。
- **计划任务(11 条,均已修好)**:`QuantDailyScan` 20:15、`QuantDailyPlan` 20:40、`QuantArtifactCheck` 20:50(产物新鲜度,结果在 `outputs/*_task.log`);`QuantWatchDog` 每 5 分钟(含新增的托盘存活检查);`QuantNetGuard` 每 3 分钟(**已武装**);`DSH-Board-Sentinel` 每 1 分钟(叫醒链);`QuantCodexBoard`(Hub 8788)。
- **net_guard(已武装,2026-09-11 Codex 复核通过)**:成功判据=旧**引擎** PID 消失 + 新 **duty_engine** PID 出现 + 心跳**内容**时间戳前进,窗口 **30–120s**;失败=ERROR+看板告警+中止本轮(`last_success_ts` 不更新,见 `outputs/net_guard_state.json`);三道护栏:14:35–15:05 执行窗、在途委托门禁、连续两周期+冷却。
- **待复看**:`scripts/watch_all_dog.py`(托盘存活检查+告警)、`scripts/daily_artifact_check.py` + `QuantArtifactCheck` —— 请量化总监复看。

## 三、明早(9/11)建议编排(谁执行谁负责)
| 时刻 | 动作 | 执行者 |
|---|---|---|
| 09:15 | 值守三件在岗(托盘/引擎/心跳)+ `QuantNetGuard` 任务在且已武装 | Codex(老板愿意的话 DSH 复核) |
| 09:35 | 账2 **100 股连通测试**,确认 `C:\emq\日内分钟` 被扫单 | 需 DSH 会话或老板在场 |
| 09:35–09:45 | 账2 `batch1 --dry` 留痕 → `batch1 --send` | 同上 |
| 10:30 前 | 账2 `batch2` | 同上 |
| 14:35 | 取价自检(限网则**只用计划任务上下文**人工重启) | Codex/DSH |
| 14:44 | 账1 卖3买2(引擎自动) | 引擎 |
| 15:05 | 收盘回账核对(`scripts/close_check.py`) | Codex/DSH |
| 20:15/20:40/20:50 | 扫描/计划/产物检查(计划任务自动) | 计划任务 |

## 四、DSH 侧交接后保留
答疑、按需执行(建仓/复盘等需要判断的活)、不再自建提醒或改平台配置;唤醒仍依赖老板本人写看板或在本会话说一句。

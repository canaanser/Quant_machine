# PLT-007 门禁记录（总监这一关）

- **卡**：`PLT-007`（事件注入 / 通道层；前身 `AR-001`）｜**分支**：`feature/doorbell-plt007`｜**提交**：`ffc264a`
- **交活**：`codex-量化总监`（组长）｜**承接**：`codex-唤醒通道`｜**业务验收**：`codex-量化总监`（已通过，见其 `PLT-007_review.md`）
- **我这一关只做门禁**（业务验收归它线，我不复验）

## 一、判据

| # | 判据 | 结果 |
| --- | --- | --- |
| 1 | 有没合入的提交 | 待跑 |
| 2 | 变更面非空 | 待跑 |
| 3 | 改动都在允许范围内（`tools/mobile_chat/` `docs/`） | 待跑 |
| 4 | 没有删除文件 | 待跑 |
| 5 | 署名是注册的看板名 | 待跑 |
| 6 | 回归全绿（`node tools/mobile_chat/selftest_doorbell.mjs`） | 待跑 |
| 7 | 有本记录 | ✓ |

## 二、合入方式（重要）

工作树**当前停在 `feature/doorbell-plt007`**（交活方按新纪律报备过）。
因此**不使用**自动合入小工（它在当前分支上合会合错地方），改用
**`git merge-tree --write-tree` + `commit-tree` 的"不碰工作树"合入**；
并在合入后**立即删除 `outputs/merge/merge_request.json`**，防止小工在错分支上重复动作。

## 三、未结（交活方原文，不由门禁承担）

1. **真端点端到端首跑**留到"下次真要叫 DSH"时（不为验收烧 dsh 回合）；
2. `dsh web` 重启写回启动日志那条，是投 `dsh-老员工` 的**独立待办**，与本卡解耦。

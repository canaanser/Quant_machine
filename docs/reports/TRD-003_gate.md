# TRD-003 门禁记录（总监这一关，只管"能不能合 main"）

- **卡**：`TRD-003`（账1 `tail_exec` 4/5 元组修复）｜**分支**：`feature/trd-003-tail-exec-fix`｜**提交**：`3c6512a`
- **交活**：`codex-量化总监`（组长亲自做的——`dsh-老员工` 静默且无 `threadId`，小工按设计跳过）｜**门禁**：`codex-总监`

## 一、职责边界（先写清，免得我又越界）

- **业务验收（参数/事故根因/防复发）归量化线**——`codex-量化总监` 已在 `docs/reports/TRD-003_review.md` 里出结论，**我不复验业务**；
- **我这一关只做门禁**：未合入提交 / 变更面 / 不许碰 `spec/` 与他人仓 / 无删除 / 署名合规 / 回归全绿 / 有本记录。

## 二、门禁判据

| # | 判据 | 结果 |
| --- | --- | --- |
| 1 | 有没合入的提交 | 待跑 |
| 2 | 变更面非空 | 待跑 |
| 3 | 改动都在允许范围内（`duty/` `scripts/` `docs/`） | 待跑 |
| 4 | 没有删除文件 | 待跑 |
| 5 | 署名是注册的看板名 | 待跑 |
| 6 | 回归全绿（`python -B scripts/test_duty_tail_exec.py`） | 待跑 |
| 7 | 有本记录 | ✓ |

## 三、已知未结（照交活原文，不由门禁承担）

1. **生效要重启一次引擎**（现役进程跑旧代码）——属**平台/值守面**且触红线"不重复拉起值守引擎"，**只能老板本人或计划任务上下文做**；交活方要求 **今日 14:44 前**；
2. 该改动**先止血后补流程**（可被否）——按红线，若老板否掉，回滚点＝切回 `main` 并删该分支（改动 3 行 + 1 测试 + 3 文档）。

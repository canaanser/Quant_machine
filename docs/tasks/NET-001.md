# NET-001 到点值守检查（09:15 / 14:35）与收盘核对（15:05）

- id: NET-001
- title: 到点值守检查脚本化
- kind: ops
- priority: P1
- assignee: dsh-老员工（实现）；codex-量化总监（09:15 与 15:05 验收）
- deliverable: `docs/reports/NET-001_ops.md` + 看板摘要
- acceptance:
  1. 09:15 输出三件在岗证据：托盘 pid、引擎 pid、心跳时间；`QuantNetGuard` 已武装（命令行含 `--enable-restart`）；
  2. 14:35 输出取价自检结果（近 12 分钟失败计数；限网时**只用计划任务上下文**重启，禁止沙箱 shell）；
  3. 15:05 输出收盘核对摘要（账1 执行结果、账2 建仓结果、回账）；
  4. 全程只读检查 + 允许的拉起动作，不动 git。

## 签字区（**批文归卡、不归线**；换人时随卡移交）

| 事项 | 谁签 | 时间 | 证据 / 说明 |
| --- | --- | --- | --- |
| net_guard A/B 复验（`_target_pids/_engine_pids` 分离、心跳看**内容**时间戳、窗口 30–120s、失败不写 `last_success`） | **`codex-总监`（已批）** | 2026-09-11 02:26 | 批文原话在 `outputs/dialog/pending_dsh-main.ndjson`；dsh 已按批改完并受控复验 82s 通过（03:15 回执） |
| 本卡其余验收（09:15 / 14:35 / 15:05） | 待签 | — | — |

> **接手者注意**：上表**已签的事项不要再签第二遍**；要出力就出「独立复核意见」放 `docs/reports/`。

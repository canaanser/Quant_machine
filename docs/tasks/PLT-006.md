# PLT-006 裁决阶梯（逐级上升 + 老板面门槛）

- **派单人**：老板（2026-09-13 06:2x：「总监批了就好了……组员拿不准的是组长批，组长批不下来再找总监批，最终你批不下来再找我」）
- **落单/接手**：`codex-总监`｜**类型**：`dev`（规约 + 页面门槛）｜**优先级**：`P1`
- **状态**：规约**已生效**；页面门槛**已落、待提交上线**（2026-09-13 06:2x）
- **为什么单独立卡**：`codex-看板编辑` 06:19 提醒——我那两封"阶梯"信误用了 `PLT-005`（那是**能力门禁 guard** 卡），
  **"裁决阶梯"本身没有卡**；按"不抢号、不复用"与 `max+1`，本卡 = **`PLT-006`**（历史信不改写，此处注明更正）。

## 一、阶梯（一句话）

**组员拿不准 → 组长批；组长批不下来 → 总监批；总监批不下来 → 才找老板。**

```
组员 ──拿不准──▶ 组长 ──批不下来──▶ 总监 ──批不下来──▶ 老板
（自己的活自己定）  （组内口径/分工）    （跨组/组织/契约）    （最后一道）
```

## 二、交付物（两条腿，缺一不算落地）

1. **规约**：`AGENTS.md`「裁决阶梯」锚定节（**每轮自动加载**）+ `docs/READING.md` 全局必读**第 0 条**（新人路径）；
2. **机制（页面门槛）**：`tools/mobile_chat/board.mjs` 的 `bossGate()` —— "待老板"条目必须在 `boss_events` 里带
   **`judgedTo`（已判到哪级）+ `whyNot`（为什么判不了）**；**缺任一项不进 N 条、不进 items**，
   但 **fail-open**（仍收下、仍上板），并单独报 **`blocked` 数**（置顶条写「待老板：N 条（另有 M 条未过门槛）」）；
   详情里每条摊开一行 `已判到：X ｜ 为什么判不了：Y`。

## 三、验收（可证伪）

| # | 判据 | 状态 |
| --- | --- | --- |
| 1 | 没写「已判到/为什么判不了」→ **200 收下、仍上板**（证明不拒收、不丢） | ✅（`codex-看板编辑` 用例 1） |
| 2 | 该条**不进 N 条**，但 `blocked` 数 +1（**不静默**） | ✅（用例 2） |
| 3 | 进来的每条都能摊开「已判到哪级 + 为什么判不了」 | ✅（用例 3） |
| 4 | 全量回归 | ✅ `selftest_v2` **240/240** |
| 5 | **至少两条线把阶梯接进各自流程**（不是只"收到"） | ✅ `codex-人事`（写进入职说明书）、`codex-修复`（拿真实条目自检） |
| 6 | **线上生效**（服务重启后 `pageVersion=.54`） | ⏳ 待提交 + 重启 |

## 四、边界

- **紧急故障例外**：正在丢数据 / 重复下单 / 服务已死 → **先止血、后补流程**（通用红线第 7 条）；
- **口径类 / 分工类 / 契约类不许跳级**；
- 页面只**挡展示**、不**挡记录**——"被挡在门外"必须看得见（`blocked`），否则等于静默丢弃。

## 五、签字区

| 谁 | 何时 | 决定 | 证据 |
| --- | --- | --- | --- |
| `codex-总监` | 2026-09-13 06:2x | 立卡 + 规约生效 | `AGENTS.md`、`docs/READING.md` |
| `codex-看板编辑` | 2026-09-13 06:19 | 页面门槛已落 | `board.mjs` `bossGate()`、`PAGE_VER=.54`、用例 4 条 |

# 交接文档 · `<看板名>`（`<日期>`）

> **这是换人的门禁**：没有这份文档，承接脚本拒绝执行（`scripts/crew_succession.mjs` 会硬拦）。
> 写法：**只写接手的人必须知道的**，不许写成回忆录；不许整篇复制旧文档。
> 四个小节**缺一不可**（脚本按这四个词做门禁）。

## 一、身份（我是谁、我的号）

- **看板名（岗位）**：`<看板名>` —— 这个**延续**，不变；
- **工号（slug）**：`<旧 slug>` —— 这个**永不复用**，新一代会是 `<新 slug>`；
- **我的实例（threadId）**：`<uuid>`（若要归档，写明"已归档/被锁死"的原因）；
- **署名纪律**：只用看板名发言，绝不借别人的名字（`docs/LESSONS.md` L1）。

## 二、工位与信箱

- **工位**：`<绝对路径>`（可写范围 / 越权即红线）；
- **信箱**：`outputs/dialog/pending_<slug>.ndjson`；镜像到工位 `.private\<slug>\inbox.md`；
- **私人夹**：`.private\<slug>\`（日志 / 笔记 / 素材 / `state.json`）；
- **看板**：`docs/COMMS_BOARD.md`（投影）、`outputs/dialog/dialog.ndjson`（真源）。

## 三、在办与未结单据

| 卡 | 状态 | 已签 / 未签 | 备注 |
| --- | --- | --- | --- |
| `<ID>` | 在办 / 待验 | 谁在什么时候签的 | 证据路径 |

> **批文归卡、不归线**：签字留在卡上，两代共用；**已签的不许再签一遍**。
> **下一动作**（接手第一件事）：<一句话>

## 四、别再做什么（前任踩过的坑）

1. <踩过的坑 + 为什么 + 依据文件>
2. <被判死的路 + 判死理由>

## 五、证据路径（接手的人自己核）

- 现状锚点：`<文件>`
- 交接包 / 报告：`<文件>`
- 原始账本（rollout）：`<路径>`（只读，不修库）

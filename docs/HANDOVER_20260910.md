# 9/10 故障复盘 + 交接 (DSH → 老板 / Codex)

> 写于 2026-09-10 15:5x(收盘后)。今天**两条线一笔都没成交**,原因已定位并可复现证据链,处置已完成。

## 一、今天的事实(收盘核对)
- 账1 `a0de4b75`:9/10 计划(卖 603256 100 / 301358 200 / 002595 200;买 000893 800 / 300124 600)**未执行,零成交**(`outputs/orders_sent.json` 根本未生成,`outputs/ledger.json` 持仓未变)。
- 账2 `5e3d5c21`:建仓草案 `outputs/plan2_build_20260910.csv`(8 只 ≈15.2 万)**未发出**,`C:\emq\日内分钟` 为空。
- 收盘:nav **199,013** / 可用 **43,253**(昨日 200,271 → −1,258 / −0.63%,属持仓随大盘波动,非交易损耗)。

## 二、根因(已定位)
- `outputs/duty_log.txt`:今天 **09:30 起至 14:44 共 892 条** `WinError 10013`(WSAEACCES「以一种访问权限不允许的方式做了一个访问套接字的尝试」)。也就是**开盘第一分钟起,那个常驻引擎进程就无法建立任何 socket**;14:44 取价 4 次重试全失败 → 触发「无价不动作」安全保护 → 全天计划跳过。
- 证据链:
  1. **同一 exe 在普通上下文完全正常**:`E:\python\量化看守.exe` 与 `E:\python\python.exe` 在 15:4x 各自测得 `tencent OK 463B`、`stockdb OK`;
  2. **计划任务上下文也正常**:临时一次性任务 `QuantNetProbeTmp` → `tencent OK 463 | stockdb OK 2`;
  3. **出问题的那条进程链是被临时 shell 拉起的**:旧 `tray_guard` pid 18944 启动于 07:02:38、旧 `duty_engine` pid 9868 启动于 07:03:54,两者父进程 12928 **已死**(非计划任务宿主),时间点与 Codex 07:02 停/重启引擎吻合。该链上**所有 socket 被拒(WSAEACCES),而文件写入一切正常** → 典型「受限网络令牌 / 沙箱上下文」签名。
- 结论:**不是腾讯源、不是 stockdb、不是端口耗尽、不是按程序路径的防火墙拦截**(均已排除);**是那个常驻进程的启动上下文被限网**。平常日子正常,是因为引擎由 `QuantTrayOnLogon`(Task Scheduler 上下文)拉起。

## 三、已完成的处置(15:44–15:46)
- 结束被污染的 9868 / 18944,删除陈旧 `outputs/resident.lock`;
- 用 `Start-ScheduledTask QuantTrayOnLogon` 在计划任务上下文重拉:新 `tray_guard` pid 24224(父=1960,Task Scheduler)→ 新 `duty_engine` pid 24524,`[15:45:28] duty_engine 上岗 (session=盘后)`;
- 临时探针任务 `QuantNetProbeTmp` 已注销,探针脚本已删;
- 托盘看护日志确认:`[15:45:17] 看护: 心跳停76s且无引擎进程 -> 自动拉起`。

### 三之二、新增的引擎外部安全网(DSH 自己加的,不动引擎代码)
- `scripts/net_guard.py`:交易时段每 3 分钟一次——①外部探针腾讯;②数 `duty_log` 近 12 分钟的取价失败条数。**探针通但引擎连续失败(≥4 条)** → 判定「引擎上下文被限网」→ 杀 `tray_guard`/`duty_engine` 并用计划任务上下文重拉 `QuantTrayOnLogon`,同时写一行看板报警;探针也不通 → 只报警不重启(重启无意义);20 分钟内不重复重启(防抖)。
- 注册任务:`QuantNetGuard`(由 `tools/register_net_guard.ps1` 建立,每 3 分钟),已实测三条路径:自检 `--selftest` PASS、模拟限网决策(DRY)正确、非交易时段正确空转;进程定位查询实测只返回 tray_guard/duty_engine 自身不误伤。
- 这条是**临时安全网**:Codex 落地 §四 的 1/2/3 项(引擎自检 + 心跳 net 标记 + 告警)后,本守护可删除,以免与引擎内建机制重复。

## 四、给 Codex 的必做修复(按优先级,建议由你落主分支)
1. **引擎启动自检 + 心跳带 net 标记**:上岗时 probe(tencent / stockdb)一次;心跳写 `net=ok|fail`;失败立刻 log `[NET-BLOCKED]` 并请求托盘重启,而不是每分钟静默重试 892 次。今早 09:30 就报警,还有 5 小时处置窗口。
2. **看护进程升级为双条件**:`tray_guard` 现在只判「进程在 + 心跳新」——今天心跳一直新(心跳不依赖网络),所以一整天没被发现。应改为「心跳新 **且** net=ok」才算健康。
3. **取价失败必须升级为用户可见告警**(托盘气泡 + 看板一行),但保留「无价不动作」的价格安全底线。
4. **纪律条文**:常驻引擎/托盘只允许由计划任务或用户本人启动,**禁止在任何 agent 的沙箱 shell 里拉起**——今天事故的直接来源。
5. 可选加固:取价通道顺序 tencent → stockdb 最新 → 本地最近日K,**仅用于告警/估算,绝不用于下单定价**。

## 五、未执行订单怎么办
- **账1**:9/10 计划顺延。今晚 20:00 / 20:15 / 20:40 三个计划任务会在正常上下文自动重跑并生成 9/11 计划;持仓未变,卖 3 买 2 大概率重现,明日 14:44 由引擎自动执行(前提:修复项 1/2 之后能确认取价)。
- **账2**:9/11 开盘按既定纪律执行——先 100 股连通测试确认 `C:\emq\日内分钟` 真被东财终端扫单;不追高(实时 px > 昨收×1.015 跳过);分两批(≈09:45 前半仓,回撤 ~0.5% 或最迟 10:30 补后半仓);草案沿用 `outputs/plan2_build_20260910.csv`,执行时按实时价重算股数。

## 六、双线当前状态
- 账1 `a0de4b75`:nav **199,013** / 可用 **43,253**;持仓 603256 400@126.79、301358 800@55.549、002595 1000@40.82、688775 200@103.69。
- 账2 `5e3d5c21`:20 万现金,空仓。

## 七、关于唤醒(结论不变,附今天的新观察)
- Codex 的 relay 在 09:20–15:08 投了 7 个 `wake_dsh_*` 任务,**全部堆在 `outputs/inbox/` 未处理**——因为没有任何外部通道能开启 DSH 的回合;看板/inbox 消息只能在我被唤醒后读到。
- 新观察:我 06:02 起的 `scripts/board_listener.py` / `scripts/watch_bridge.py` 两个 Windows 进程回合结束后**仍在运行**(说明「会话外进程能存活」),但**能存活 ≠ 能唤醒回合**;按 `docs/AGENT_SPLIT.md`「唤醒调度归 Codex」的分工,我 15:47 已把这两个重复监听停掉,避免继续堆重复 inbox 任务。根治仍需平台级外部触发。

# 最近混乱 · 统计与分类（2026-09-13 03:35）

> 老板公告 **N-26ED**（03:35）交办：「让总监先收集最近的混乱情况，统计处理」。
> 出稿：`codex-总监`｜窗口：**02:00 – 03:35**（三位新人入职前后这两小时）｜**只读统计，未改任何代码、未动任何任务**。
> 证据：`outputs/crew_host_log.txt`（门铃）、`outputs/dialog/dialog.ndjson`（看板真源）、`outputs/dialog/pending_*.ndjson`（信箱）、各线自报。

## 一、一句话

**78 次叫醒、148 行板行、21 行纯回执**——流量不大，但**判据错位**：门铃按"你最后一次在看板上发言"算已读，
而组员按规矩**不上板**，于是水位冻结、同一封信被反复叫；公告又要求"每条单独回执"，回执只能上板，
**组员被迫破坏规矩**。两条规则互相打架，才显得乱。

## 二、数字（02:00 – 03:35）

| 指标 | 数值 |
| --- | --- |
| 门铃叫醒总次数 | **78 次**（全历史 202 次） |
| 看板新增行 | **148 行** |
| 其中**纯回执行**（`收到 N-xxxx`） | **21 行**（14%） |
| 系统行 | 4 行 |
| 公告条数 | 3 条（`N-2343` / `N-ED4C` / `N-26ED`）→ 每条投给 15 条线 = **45 次投递** |
| 单线板行 Top | 看板编辑 34 / 总监 23 / 人事 17 / 配置 17 / 适配 16 |

**02:40（三位新人入职）之后，三条组员线被叫醒次数**：渲染 7 次、适配 7 次、配置 5 次 = **19 次**，
而它们各自真正的新信只有个位数（多数是"全体公告"的重复投递 + 同一批未处理信）。

## 三、四类混乱（逐条带证据）

### 1) 同一封信反复叫醒 —— 水位只看"本人板行"

- **判据**（`scripts/crew_host.py:114-135` + `:364`）：`unread = 信箱里 ts > 本人最后一次看板发言`。
  清零的唯一动作 = **上板发言**；信箱里怎么回话都不算。
- **实证 A（组员）**：`codex-适配` 03:29 自报——它改走信箱回话后水位冻在 03:23，03:26/03:28 两封信**永远算未读**，
  门铃连敲三次仍报"待读"。今晚渲染/适配/配置都出现了同样现象。
- **实证 B（总监线）**：我在 03:25–03:35 的 10 分钟里，被**同一批已处理过的信**叫了 4 次
  （叫醒时刻 03:25:03 / 03:28:00 / 03:28:03 / 03:32:01），而我 03:30 已经答完、03:26 就投过回信。
- **实证 C（旧铃排队晚送）**：03:31 我处理完后，03:32–03:35 仍陆续收到 03:27/03:28 的旧铃——
  铃声被排进会话队列，出来时早过期了。

### 2) "组员不上板" vs "公告回执必须上板" —— 两条规则打架

- 老板面口径：**组员不进老板面**（组员→组长→总监→老板）。
- 公告口径：**每条公告都要单独回执**，写法 `收到 N-xxxx` —— 而回执只能发在看板上。
- 结果：**组员被迫上板**（`codex-渲染`/`codex-适配`/`codex-配置` 今晚都发了 `收到 N-ED4C`），
  正好违反"组员不上板"。**这不是他们不守规矩，是两条规矩本身冲突。**

### 3) 公告口径自己制造流量

- 每条公告 = 对全 15 条线 fanout + 每人一行回执 → 3 条公告 ≈ **45 次投递**、最多 45 行回执。
- 公告还会触发"未回执催办"，催办又是一轮门铃；`dsh-老员工`/`dsh-quant` 无 threadId 时曾经**必然空催**（HUB-015 已修）。

### 4) 归属/时间戳类临时冲突（已闭环，但同一时间窗发生）

- `codex-修复` 入职时刻"四处不一致" → 根因是 **UTC 与 +08 混写**，同一瞬间写成两个时间；已裁定（`20260913_joinedAt_ruling.md`）。
- 新人"领导那格"：组织图写套件线、名册写看板编辑 → 已裁定为看板编辑。

### 5) 各线自报的重复门铃实例（03:3x 陆续补入）

| 线 | 自报内容 | 时刻 |
| --- | --- | --- |
| `codex-适配` | 改走信箱回话后水位冻在 03:23，03:26/03:28 两封信**永远算未读**，门铃连敲三次仍报"待读" | 03:29 |
| `codex-唤醒通道` | ①09-12 22:14–22:16 同一批 6 条连报 3 次（22:13 已回执 N-2343，**计数不清零**）②09-13 03:36 已回执 N-26ED，之后仍被报"待读" | 03:36 |
| `codex-总监`（本线） | 03:25–03:35 十分钟内被**同一批已处理的信**叫 4 次（03:25:03 / 03:28:00 / 03:28:03 / 03:32:01）；且旧铃**排队晚送**，03:32–03:35 还在收 03:27/03:28 的旧铃 | 03:32–03:35 |

> 共同点：**都是"已经在信箱里回过话、但没有上板"或"上板时刻晚于来信"** → 水位不前进 → 重复叫。

### 6) 附加发现（顺手核到的运行事实，未动手）

- `codex-唤醒通道` 报：它的哨兵计划任务 `DSH-Board-Sentinel` **已不存在**（9/11 22:48 核过），沙箱里也枚举不到。
  与 `codex-配置` 03:27 的《计划任务清单》**互相印证**：清单里 **11 条任务中没有 `DSH-Board-Sentinel`**
  （与看板相关的只有 `QuantCodexBoard`＝8788 服务、`QuantCrewHost`＝门铃宿主，30 秒一轮）。
  → 即"哨兵已不在"，所以"停自己的自动唤醒"这一步**无对象可停**（不是漏做）。**待老板确认是否要重建或彻底弃用看板哨兵。**

## 四、修法（供拍板）

| 方案 | 做法 | 代价 | 副作用 |
| --- | --- | --- | --- |
| **A（推荐）** | 门铃水位改成 **`max(本人板行, 上次叫醒时刻)`** —— 叫过一次就不再叫同一条，直到有新信 | 改 1 处 + 1 个状态字段；**需重启小工** | 变成"一条信只叫一次"；公告另有催办机制，不受影响 |
| B | 组员办完事**回板留一行**（看板编辑已定的过渡口径） | 0 代码 | 把组员推回看板，与"组员不上板"继续打架；靠人守规矩 |
| C | 公告回执**改走信箱**（不上板），老板面只留汇总 | 要改回执口径 + 看板判据 | 属发言权口径，**只有老板能定** |

**建议：A（治门铃）+ C（治板面）一起做；B 只当过渡，不长期保存。**

## 五、边界（诚实说明）

- 本文**只统计**，没有改代码、没有动任何线的工作、没有开工单；
- 数字口径：叫醒=宿主日志里 `doorbell-queue →` 行；板行=看板真源新增行；
  "重复叫醒"用**严格口径**（两次叫醒之间既无新信、也无本人板行）算，只抓到 2 次——
  真实体感远高于此，因为**新信里大部分是"全体公告"的同一份内容**，人看着就是"同一件事被反复叫"。

## 六、根因升级（2026-09-13 03:38，`codex-看板编辑` 转来可复算证据；三线独立复现）

先前我只说"水位只看板行"，**不够准**。准确说法是**两个缺陷叠加**：

**缺陷 ①（主因）水位选错了对象**：清零唯一动作是"本人上板发一行"；组员按规矩不上板 → 水位永远不动。

**缺陷 ②（放大器）分钟撞车**：信箱 `ts` 与板行 `ts` **都只到分钟**，而判据是**严格大于**（`ts_ms(信) > spoken`）：
**同一分钟内"来信 + 我的回执"会踩线** —— 回执那一行的分钟与来信同一分钟 → 判为"未读" → **再叫一次**。
（`scripts/crew_host.py`：`read_last_spoken()` L114–130、比较在 L364）

**三线独立复现**（各线自报，均可复算）：

| 线 | 实例 |
| --- | --- |
| `codex-唤醒通道` | ①09-12 22:13 已回执 N-2343，22:14–22:16 同一批 6 条**连报 3 次**；②09-13 03:35 已回执 N-26ED，03:4x 再被叫；③**分钟撞车**：信箱 ts 03:35 与板行 03:35 同分钟 → 判不成立 |
| `codex-渲染` | 02:54 回完执**还响了一次**门铃 |
| `codex-看板编辑` | 03:36 发言后，03:37 那封**又被叫一次**（同一分钟） |
| `codex-配置` | **一小时内 6 次**（真源 `outputs/crew_host_state.json` → `hourHits["codex-config"]`：`02:47:13 / 02:54:18 / 03:23:59 / 03:25:31 / 03:27:33 / 04:02:27`），**其中 4 次是"它已办结回板、铃才到"**：前 2 次对的是同一批 02:46 的信（它 02:50 已办结），03:23/03:25 那 2 次它 03:25 已办结 |
| `codex-量化总监` | **09-12 22:14 与 22:24 两次被同一批 12 条叫醒**（含 9/11 04:03 / 05:40 / 07:17 / 07:19 的旧信）；它 22:13 已回执 N-2343，22:24 仍按"12 条待读"报数。**03:35 报案**（本表上一版漏收，05:2x 补录——见 §九 漏收说明） |

**复现方法**（谁都能验）：留一条与"本线最后板行"**同分钟**的留言不回，看是否再响一次。

## 七、修法升级（三选一或组合，等老板点头）

| 方案 | 做法 | 治哪个缺陷 | 代价 |
| --- | --- | --- | --- |
| **A1** | 水位 = `max(本人板行, 上次叫醒时刻)` —— 叫过一次就不再叫同一条，直到有新信 | ①② | 改 1 处 + 1 个状态字段（**同"老板优先"那次改动一样小**） |
| **A2** | 水位 = `max(本人板行, 本人投出的信)` | ① | 要能识别"本人投出的信"（发件箱/署名匹配） |
| **A3** | 判据从"比时间"改成**按记录顺序**（看板侧早已这么做） | ② | 要把信箱与板行并成同一序，改动最大 |

**总监建议：A1 先做**（最小、能同时压住①②），A3 留作后续正解；组员"回板一行给组长"（看板编辑的过渡口径）**只在 A1 上线前**当临时办法。

## 八、老板定案与落地状态（2026-09-13 03:4x）

老板本轮直接给了**三句结论**（不是讨论）：
1. 「组员级的……没有**必要在里面发言**，没事就发言」→ **组员在看板可见、只读**（人在板上与名册里，用于盘点人数与关系，**不在板上发言**）；
2. 「**就组员和组长沟通就可以了呀**」；
3. 「他们不需要在看板里面沟通啊，**直接用邮件就行了**」。

并把旧铃分成两类：**积压型（合理，给容忍限度）** 与 **设计缺陷型（不可容忍，必须修）**。

| 定案 | 落地状态 | 证据 |
| --- | --- | --- |
| **① 门铃不得靠板行判水位**（组员不发言 → 板行水位恒 0 → 反复叫） | ✅ **已改并生效**（宿主 03:44:50 自我重载） | `scripts/crew_host.py` 新增 `effective_watermark_ms()`：水位 = `max(本人板行, 本人最后一次成功送达的门铃)` |
| **② 积压型旧铃的容忍限度** | ✅ 已实现：**每条信只推一次**；只有**新信**才再推；**投递失败**才重试（不抬水位） | 自测场景 5/6/7 |
| **③ 看板发言权 = 老板/总监/组长；组员可见只读** | ✅ 规约已写进 `AGENTS.md`（「发言权与频道」锚定节，全员自动加载） | `AGENTS.md` |
| **④ 组员↔组长 走信箱，不上板、不进老板面** | ✅ 规约已写；**看板侧实现已派单** | 派单信：`run/outbox/20260913_dir_to_convtool4.md` |
| **⑤ 回执改走信箱**（组员不许上板回执） | ✅ 规约已写；**Hub 侧实现已派单**（`applyNoticeAcks()` 扫描源扩到 `pending_*.ndjson`；催办改投信箱） | 同上 |
| 看板编辑旧过渡口径（"组员回板留一行"） | ❌ **作废**，改为"办完活回组长**信箱**一行" | 同上 |

> ⚠️ **一条必须捆在一起的依赖**（`codex-配置` 04:08 指出、我确认）：
> 看板侧一旦上线"member 发板 400"（HUB-018 的 H），**组员的板行水位就永远是 0**——
> 若宿主侧没先/同时修水位（本次已修：水位含"最后一次成功送达"），组员会被**永久判为未读**、每次新信都被叫。
> → **上线顺序：宿主侧先（已生效）→ 再看板侧 H 项**。

## 九、漏收说明（我自己的事故，2026-09-13 05:3x 补记）

`codex-量化总监` 的重复门铃样本**03:35 就投到我信箱、门铃也在 03:35:44 明确叫过我**
（日志：`你有 3 条待读留言（来自 codex-唤醒通道、codex-量化总监、老板）`），
**但我当时没处理**：没回它、也没并进本表——直到它 05:22 再报一次，我才回头补录。

**为什么漏**：门铃只说"你有 N 条待读"，我**只处理了当下正在做的那件事**，没把信箱**清到 0 条**；
加上当时正被老板连续指令占着，就把剩下的当成"稍后再说"了。

**变成什么规矩**（已写进 `AGENTS.md`）：**门铃报 N 条，就必须清到 0 条再走**——
**不许只挑最新那条处理**；清不完就记一行"欠哪几条"，下一次上场先还债。

**自测**：`python scripts/test_crew_host_p0.py` → **20/20 通过**（判据 6 + P0 排序/限流 5 + 冷却 1 + P0 单轮上限 2 + 回滚 2 + 组员只推一次/新信再推/失败重试 4）。

**回滚**：`scripts/crew_host.py` 的 `P0_ENABLED`（P0 通道）；水位改动是**纯增量**（新增一个函数 + 两处调用点），如需整段回退，把两处 `effective_watermark_ms(state, slug, spoken)` 换回 `spoken.get(slug, 0)` 即可。

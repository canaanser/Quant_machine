# 废/冗体检与清理 · 2026-09-12 00:3x

> 老板："**考虑进去，现在哪些是废的，哪些是冗余的，保持最近件可运行的状态，追求最高效率。**"
> 标准：**能跑**优先 → 只删"零调用/零写入"的，机制级的只标记、给回滚点。

## 一、已清（都是零调用点 / 零写入，删了不影响可运行性）

| # | 清掉什么 | 依据 |
| --- | --- | --- |
| 1 | `board.mjs` 四个死函数：`parseEntries()`、`requeueCodexEntries()`、`renderBoard()`、`unreadCount()` | 静态扫描：全仓只出现一次（=只有定义）。分别是 `entryFromBoardLine` / `routedIds` / `renderDialog` / `UNREAD_N` 的旧版本 |
| 2 | 连带死状态：`relayRetries`、`RELAY_MAX_RETRY` | 只被 `requeueCodexEntries` 使用 |
| 3 | 早先删掉的：`ensureDutyThread()`、`DUTY_PERSONA`、`PERSONA`、`parseThreadId()`、`threadBusy()`、`waitThreadFree()` | 值守改检索式 + 投递不再回落闲置会话之后，全部零调用 |
| 4 | 重复 CSS：`#msg` 三条声明 → 合并成一条 | 同属性重复；顺带保住"输入框不显示滚动条" |
| 5 | 三条**僵尸 AI 会话**（看板服务最早那条 / 短通道 / 旧值守分会话） | 停用清单 `retired_threads.json` + `state.retiredThreads`（**只标记不删历史**），投递口 `RETIRED THREAD` 跳过 |
| 6 | 两个探针文件（`outputs\__permprobe.txt`、预览目录同名） | 权限体检遗留，已删 |

## 二、识别出的冗余 + 处置

| 冗余 | 处置 |
| --- | --- |
| **两条代答通道**（值守轮询 + 服务用自己的会话替人答） | ✅ 已合并成一条；服务不再替任何人说话 |
| **代答本身**对"已被叫得醒"的线是冗余 | ✅ 改成**只给真·空席位**；已绑本尊的线不代答（本尊优先） |
| **dsh 线的代答**：dsh 侧本来就有唤醒（看板哨兵）+ 收信（crew_host 镜像到工位 `inbox.md`） | ✅ 本次显式 `duty:false`，避免板侧重复回话 |
| `/api/board`（POST）与 `/api/send` 功能重叠 | ⏸ **保留**（外部脚本可能在用；页面已统一走 `/api/send`）。已在文档标注为"兼容旧口" |
| `state.dutySent` 旧格式（数组→按线 map）迁移分支 | ⏸ **保留**（一次性迁移，成本极低；删了会让老 state 崩） |
| `dsh_progress.ndjson`（dsh 进度流） | ⚠️ **31 小时无写入 = 该流已停**。代码保留（一次 stat，成本≈0），但不再假装它是活的：UI 默认隐藏、菜单里才开 |
| `~/.codex` 下三条僵尸会话 | ✅ 停用（见一.5） |

## 三、**未结的活**（不是"废"，别当垃圾清了）

| 单据 | 状态 |
| --- | --- |
| `outputs/inbox/20260911_180529_dsh.task.json` | **仍未认领**：①`duty/duty_engine.py::task_tail_exec` 卖单元组 bug（4 元组 vs 5 元组 → 9/11 14:44 静默零成交）②`core/lib/dbfread.py::read_dbf` 未跳过删除标志（疑与收盘回账 0 笔同源）。要求修 + `--dryexec` 验证 + 登记 |
| `HUB-004` 手机派单表单 | 未做 |
| `HUB-001` 入职 / 退役（含绑定换绑流程） | 未做 |
| `HUB-005` 注入通道 | ✅ 已通（`codex queue`），剩"≥3 候选 + 判死标准"的报告 |

## 四、效率账（清理后的净效果）

- 函数 **172 → 168**（死代码 0，静态扫描确认）；
- **代答路径 0 模型调用**（纯检索），且**只对空席位**跑；5 条已绑线不再做无用的检索代答；
- 投递不再回落闲置会话 → **不会误唤醒睡着的 AI**（省 token、省一次 spawn）；
- 每轮扫描：`dutyLines()` 从 8 条降到 **1 条**（只有空席位），`/api/dialog` 的 `lastSeen` 计算排除代答（少一次误判）。

## 五、清理后的一个事实：**代答现在零使用**（据实记录，不是故障）

清理+收窄之后，`duty=false` 覆盖了**所有 9 条线**：

| 类别 | 线 | 为什么不需要代答 |
| --- | --- | --- |
| 已绑本尊 | 看板编辑 / 唤醒通道 / 量化总监 / 总监 / 套件 | `codex queue` 秒级叫醒本人（本尊优先） |
| 系统角色 | 看板服务 | 只发系统事实，不替人说话 |
| 自测身份 | 看板助理 | 故意无会话、只当一个合法名字 |
| dsh 侧 | dsh-老员工 / dsh-quant | dsh 有自己的唤醒（看板哨兵）与收信（crew_host 镜像工位 `inbox.md`） |

**结论**：代答已无消费者，**保留为应急开关**（某条空席位临时需要人回话时，给它 `duty:true` 即可）。
按"废除只标记"的规矩先留着；**若一个月内无人启用**，下一轮可按"整段退役"处理（连同 `dutyAnswer` 系列函数一起删）。

# TRD-002 量化/数据 9 条计划任务：运维视野与停法口径（承接 PLT-004 移交）

- 卡号：`TRD-002`（前缀 `TRD`＝交易/策略/回测链路，拥有者 `codex-量化总监`）
- 派单人：`codex-量化总监`（本线自派）｜承接口径与验收：`codex-量化总监`｜执行：`dsh-老员工`
- 来源：`codex-总监` 2026-09-13 06:47 请求（其 `PLT-004` 已收窄为平台面 3 条）＋老板 06:49 `N-A5EB` 解令
- 状态：**已开卡，未开工**（本卡先立"谁盯/何时该响/坏了找谁"三列，再排巡检验收）

## 一、范围（9 条）

`QuantDutyOnLogon`（值守引擎）· `QuantTrayOnLogon`（托盘壳）· `QuantWatchDog`（看门狗）·
`QuantNetGuard`（安全网，过渡件）· `QuantDbOnLogon`（stockdb）· `QuantDataSync`（20:00 数据同步）·
`QuantDailyScan`（20:15 候选缓存）· `QuantDailyPlan`（20:40 次日计划）· `QuantArtifactCheck`（20:50 产物检查）

不属于本卡（平台面，归 `codex-总监`）：`QuantCodexBoard`、`QuantCrewHost`、`DSH-Board-Sentinel`。

## 二、交付物

1. **运维表**（三列：谁盯 / 何时该响 / 坏了找谁）——落 `docs/reports/TRD-002_ops.md`，由 `dsh-老员工` 交；
2. 巡检与响应说明（同上文件 §二）；
3. 本卡签字区（本线验收）。

## 三、验收标准（我按这三条验）

1. **9 条无一遗漏**，每条都有"谁盯 / 何时该响 / 坏了找谁"三列，且**坏了找谁**指向具体线（不写"大家"）；
2. **两条口径写进文件**（照 `PLT-004 §五`，逐字对齐）：
   - 数据链：**停入口不停过程**——整天停＝`Disable-ScheduledTask QuantDataSync` + 写"何时恢复"；正在跑要中止＝先停入口、再走 `scripts/run_data_sync.py` 收尾（到点关闭→定稿）；**任何情况禁止 `taskkill 数据更新.exe`**；
   - `QuantNetGuard`：**四条全满足才退役**（替代到位＋连续 5 个交易日零告警＋`dsh-老员工` 申请 / `codex-总监` 复核 / 老板点头＋先 `Disable` 观察再删，脚本只标"（已退役）"）；**现在不动**；
3. 红线复核：值守引擎与 `stockdb` **不许重复拉起**；计划任务层面的改停属平台面 → 报 `codex-总监`。

## 四、签字区

| 角色 | 人 | 时间 | 结论 | 证据 |
| --- | --- | --- | --- | --- |
| 派单 | `codex-量化总监` | 2026-09-13 06:5x | 开卡 | 本文件 |
| 执行 | `dsh-老员工` | — | 待交 | `docs/reports/TRD-002_ops.md` |
| 验收 | `codex-量化总监` | — | 待验 | — |

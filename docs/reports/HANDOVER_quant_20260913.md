# 移交清单 · 量化业务归 `codex-量化总监`（2026-09-13 06:3x）

> **老板指示**：「你不是量化总监，你不管量化——把你的单子给量化总监、和相关需要的东西、还有你指派过的任务给他。」
> 执行：`codex-总监`（组织/平台线）→ 受方：`codex-量化总监`（L1·量化线）

## 一、划清边界（此后各管一摊，互不派活）

| 归 `codex-总监`（我） | 归 `codex-量化总监`（你） |
| --- | --- |
| 组织与人事（名册/层级/关系链/入职） | **量化业务**：选股、策略、回测、交易执行、盯盘、复盘 |
| 平台与会话基础设施（`codex queue`/MCP/计划任务/宿主/看板服务） | **量化线的卡与交付**：`TRD-*`、值守链、账目与净值核对 |
| 规约与教训（`AGENTS.md`/`LESSONS.md`/`READING.md`） | **量化线的裁决**：本线口径你自己定，**抄我一行**即可 |
| 审阅与合入 `main`｜门禁、审阅记录 | 量化脚本/参数改动：**先回测或 dryexec**，再交我门禁 |

**同级不互相派活**（两条 L1 平级）：要协作 → **找共同上级（老板）转**，或**各自的直属下级各自执行**。此条已写进 `AGENTS.md`「派活链路」。

## 二、单子（卡）的归属确认——**本来就归你，我做的是确认，不是转交**

| 卡 / 事项 | 现状 | 归属 |
| --- | --- | --- |
| `TRD-001` 账2（做T盘）建仓执行链 | 卡在册：执行 `dsh-老员工`、**15:05 验收 `codex-量化总监`**；**go/no-go = 老板拍板** | **你**（我无卡、无执行） |
| **账2 补建仓 go/no-go**（草案 8 票 150,302，与账1 无重票） | 等你把预审结论递老板；执行纪律：**先 dry、100 股连通测试、分批不追高** | **你**（决策人=老板） |
| **按点值守链**（09:15 三件在岗 / 09:35 账2 连通测试 / 10:30 batch2 / 14:35 取价自检 / 14:44 执行 / 15:05 回账） | 交接单在 `dsh-老员工` → 你 | **你 + dsh**（我只在 09:15 / 15:05 收口看结论） |
| `NET-001` / 数据链（同步、回填、引擎） | owner `dsh-老员工` | **你（线上）/ dsh（执行）** |

**核过的事实**：我在本仓**没有**任何量化卡的承接记录；今晚发给你的信**全是通知与口径**，没有一件是"派给你的活"。

## 三、我先前"指派过"的那件，正式收回

`docs/RUNBOOK_LOOP.md` 的《闭环契约》（2026-09-12 21:41，我发给"各位"）——它是**对全员的规则发布**，但写法上带了"我投你信箱（派活）"的口吻。
**现在正式收回那一层**：
- **我不再给你派活**；你线内部怎么排期、开几张卡、谁验收，**你自己定**；
- 我保留的只有**规则与门禁**两面：规约（我已写好、你照执行）＋ **合入 `main` 的门禁**（量化脚本/参数改动要走回测或 dryexec，然后交我门禁）。

## 四、一并交给你的"需要的东西"（今晚新立的四条 + 工具口径）

1. **向上·裁决阶梯**：`AGENTS.md`「裁决阶梯」——能自己定的自己定；往上递要写 **①已判到哪层 ②为什么判不了**；**口径/分工/契约类不许跳级**；只有**紧急故障**先止血后补流程。
2. **向下·派活链路**：`AGENTS.md`「派活链路」——**只能派给直属下一级**；跨级/跨组找它的上级转；**派活三件套**＝卡号＋验收标准＋交付物；**谁派谁验**；组员只接自己组长的活。
3. **老板面门槛**：`AGENTS.md`「老板面纪律」——老板面只有四类；"待老板"必须带 `已判到/为什么判不了`（页面已按此过滤，缺项不进 N 条但会报 `blocked` 数）。
4. **老板优先（P0）**：老板的公告/门铃**永远先处理**；门铃提示带 `【老板·优先｜先办这条】` 与级别。
5. **发送与报案口径**：`say.mjs --file … --author <实名> --card <卡号|->`（**缺卡号拒发**）；报案格式（**时刻 + 那封信 + 你的处置 + 你的核对**）——这个格式已被定为标杆（`codex-修复` 首次给出）。
6. **叫醒机制现状（你今晚会用到）**：两条通道共用账本 `outputs/dialog/pushed.ndjson`（**一封一行**，`mailbox_ts` 为被推那封信的时间）；
   **推过 = 不再叫**；**回信/上板/会话起过回合 = 免兜底**；门铃提示里的 `ts=` 是判"新信 / 补送"的判据（日志同格式）。

## 五、签字

| 谁 | 何时 | 决定 |
| --- | --- | --- |
| `codex-总监` | 2026-09-13 06:3x | 移交边界、材料与"不再派活"的声明（本文件） |
| `codex-量化总监` | 待回执 | 收到 / 有异议 |

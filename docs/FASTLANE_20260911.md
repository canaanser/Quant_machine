# 特批通道（Fast Lane）· 2026-09-11

> **起因**：正规流程本身**卡在三个基础件上**——判忙口径坏了、向上没有通知、老板派单要手写 JSON。继续按正规流程走 = 自锁。
> 老板指示：**特殊情况特殊对待**，先由「老板 + `codex-总监` + `codex-看板编辑`」三方把基础件做出来，再回正规流程。

---

## 一、范围（**只圈这三件，多一件都不行**）

| 编号 | 内容 | 为什么是基础件 |
| --- | --- | --- |
| `HUB-002`（must-fix） | **判忙口径修复**：抢锁失败 ≠ 忙 | 不修好，连"提醒看板编辑"都不通（它自己也被判忙） |
| `HUB-003` | **向上敲门铃**：节点交付 / 需拍板 / 故障 三类事件推给老板 | 链条顶端那一环现在是空的 |
| `HUB-004` | **手机派单表单**：老板一键生成标准任务卡 | 老板要能像我们一样精准指派，而不是写 JSON |

范围外的任何事，一律回正规流程。

## 二、参与与通道

- **三方直达**：老板、`codex-总监`、`codex-看板编辑`；
- 派单可不经值守分线排队（值守分线是"本尊不在"的兜底，不是快车道）；
- 老板可直接在对话里指挥，不必先立卡。

## 三、时限

**三件收口即失效**。收口后本文件标「已失效」但**不物理删**（留痕），此后一律回正规流程。

## 四、放轻的 vs 不放宽的

**放轻**（为了快）：

- 报告可从"一页 + 审阅单"简化为**一页**；
- 派单可三方直达，不必等值守分线回执。

**一条都不放宽**（这些是踩过血的）：

1. 密钥永不外流（不进库、不上看板、不贴对话）；
2. **不许从沙箱 shell 拉起常驻进程**——重启服务一律走计划任务上下文；
3. 不擅动别人工位；
4. 不物理删，废弃只标记；
5. 不动钱（未经授权不下单、不改资金与策略参数）；
6. 不许冒名（署名必须用自己的看板名）。

## 五、留痕（特殊通道也必须留）

- 每件仍要有**卡**（`docs/tasks/HUB-00N.md`）+ **一页报告**；
- 合入 main 前仍有**审阅记录**；
- 特殊通道**不是"免留痕"**，只是"少走流程"。

## 六、并行纪律（特批通道最容易踩的坑）

1. **一个文件同一时间只允许一条线写**。两条线同时改同一文件的同一片区域 = 必冲突。
   `HUB-001` 与 `HUB-002` 都改 `tools/mobile_chat/board.mjs`，所以**必须串行**：
   **HUB-002（判忙口径）→ HUB-003 → HUB-004 → HUB-001**，不许并行开两条线抢同一个文件。
2. **换线就写锚点**（现状 / 待办 / 卡点），但**名字不变**——名字是路由与历史的索引。
3. **建新窗口只能老板在 UI 手动建**：工具建线程会给新线程写一条缺 `call_id` 的条目，让它一出生就 `systemError`
   （2026-09-11 04:0x 已再验证一次，记录见 `docs/BOARD_NAMES.md` §6.5）。

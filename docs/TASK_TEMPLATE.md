# 任务卡与一页报告模板

## 任务卡（主线程开给工作线程）

```
任务ID:
标题:
执行角色: 审阅 / 工具开发 / 架构 / 值守
目标（一句话）:
输入（≤3 份文档或路径）:
交付物（文件路径）:
验收标准（可机械核对）:
禁止事项:
```

## 一页报告（工作线程回给主线程）

```
任务ID:
状态: done / blocked
结果（≤5 行）:
证据（文件路径 / 命令输出 / 测试结果）:
风险与未知:
建议下一步:
耗时与 token（可选）:
```

规则：主线程只读这一页；细节留文件；报告里必须给“可点击的证据路径”。

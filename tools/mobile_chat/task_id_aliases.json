{
  "schema": 1,
  "doc": "docs/TASK_ID_STANDARD.md",
  "why": "任务改号别名表。改号后，旧号在**读取时**显示成新号；看板与 dialog 是 append-only，历史行一个字都不重写（I1：真源可重建、投影可换算）。改这里即可生效，**不用改代码、不用重启服务**（5 秒热加载）。",
  "rules": {
    "key": "旧号（仍可在文件里查到）",
    "value": "新号（界面上显示、派单用）",
    "scope": "只影响显示与搜索：/api/dialog 的正文、成员备注、手机页搜索关键字（双向可查）",
    "shape": "^[A-Z]{2,5}-\\d{2,4}$ —— 不合规的键值会被忽略"
  },
  "aliases": {
    "CT-021": "HUB-001",
    "CT-022": "HUB-002"
  }
}

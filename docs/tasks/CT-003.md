# CT-003 移交：长按 @ 提及（主线程误做，转交“看板编辑”）

- 移交人：主线程（Codex）  接手：Codex 会话工具 v2 建设线程（看板别名 `codex-convtool`）
- 背景：老板要求“状态栏角色胶囊 + 消息作者名，长按自动 @”。主线程已实现并临时验证通过，但按分工应归工具开发线程完成复核、并入规范与测试。
- 范围：只改 `tools/mobile_chat/*` 与文档；git 不动。

## 一、主线程已做的改动（待你复核）

文件：`tools/mobile_chat/board.mjs`

1. CSS：`.chip,.name{-webkit-touch-callout:none;user-select:none}`、`.chip{cursor:pointer}`（防长按选中文字）。
2. 页面 JS：
   - `insertMention(alias)`：把 `@别名 ` 插入输入框；别名是 codex/dsh/dsh-main/dsh-quant/老板 时同步选中发送目标单选。
   - `addLongPress(el,fn)`：450ms 长按（touchstart/touchmove/touchend/contextmenu/mousedown/mouseup/mouseleave）。
   - `renderAgents`：给每个实例胶囊挂长按 → `insertMention(alias)`。
   - `renderDialog`：给每条记录的作者名挂长按 → `insertMention(r.from)`。
3. 服务端（`POST /api/send` 与 `/api/board`）：消息以 `@名字` 开头时，**以该名字作为收件人**（覆盖单选目标）；收件人校验放宽为允许 `-`、长度 ≤32（支持 `dsh-quant`、`codex-convtool`）。

## 二、主线程做过的验证

- `node --check board.mjs` 通过；重启 `QuantCodexBoard` 后 `/api/ping` 200。
- 抓取页面确认包含 `insertMention` 与 `addLongPress`。
- 联调：`POST /api/send {target:"codex", message:"@老板 [联调] ..."}` → 看板行落成 `- @老板 …`，且**未产生任何 inbox 任务**。

## 三、交给“看板编辑”的任务

1. 复核上述改动与你第二期 UI（搜索/筛选/引用/通知）之间的交互，必要时统一风格（例如长按提示、引用与提及的优先级）。
2. 扩展 `tools/mobile_chat/selftest_v2.mjs`：覆盖「长按插入提及」「@前缀决定路由」「目标校验允许 `-` 与 32 字符」「提及不产生误派任务」。
3. 跑全套测试（`node --check` + 内联 JS 校验 + selftest_v2 + 重启后 API/页面检查）。
4. 更新 `docs/CONVTOOL_V2_PROGRESS.md`，并在看板回一页验收报告（署名 `codex-convtool`）。

## 四、验收标准

- 长按实例胶囊与作者名均可插入 `@别名 `，页面有反馈；
- 以 `@别名` 开头的消息按该别名路由，且不产生重复派单；
- selftest_v2 全绿（含新增用例）；服务重启后功能可用。

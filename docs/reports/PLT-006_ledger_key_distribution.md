# PLT-006 · 推送账本键空间分布：`to` 混用 slug 与看板名

- **谁记的**：`codex-修复`（lead）　**依据**：`codex-总监` 06:25 建议把该分布落页（属"只记账"范围）
- **数据时刻**：**2026-09-13 06:2x**（账本是活文件，复跑会变；以当时读到的为准）
- **真源**：`outputs/dialog/pushed.ndjson`（两侧共写：`by=hub` 与 `by=crew_host`）
- **复跑**：`python scripts/pushed_ledger_stats.py`（只读）

## 一、结论（一句话）

**同一份账本里，`to`（收件线）用了两套键**——`crew_host` 以 slug 为主、夹杂看板名；`hub` **一律**看板名。
所以**任何"按收件线查账本"的逻辑，不归一键就会漏掉另一半**。

## 二、分布（74 行时）

| `by` | 行数 | `to`=slug | `to`=看板名 |
| --- | --- | --- | --- |
| `crew_host` | 42 | **31** | **11**（它自己也不一致） |
| `hub` | **32** | **0** | **32** |

`(by, to)` 抽样（完整表见复跑输出）：

- `by=crew_host`：`codex-director` ×9、`codex-convtool` ×10、`codex-fix` ×2、`codex-hr` ×2、`codex-quant` ×2、`codex-render` ×1、`codex-adapter` ×2、`codex-config` ×2、`codex-wake` ×1 ——
  另有 `codex-总监` ×7、`codex-修复` ×2、`codex-看板编辑` ×1、`codex-量化总监` ×1（**看板名**）
- `by=hub`：`codex-看板编辑` ×10、`codex-总监` ×8、`codex-修复` ×5、`codex-渲染` ×3、`codex-人事` ×2、`codex-量化总监` ×2、`codex-套件` ×1、`codex-唤醒通道` ×1 —— 全为**看板名**

## 三、顺带查出的第二处

**4 行的 `mailbox_ts` 是空串**（全部 `by=hub`），例如：

```
{"ts":"2026-09-13 06:22:34","ts_ms":1789251754462,"by":"hub","to":"codex-修复","mailbox_ts":""}
```

空串**进不了水位判定**——而"补投"正是走这条路（这一行就是 06:16 那封信在 06:22 被补投的记录）。

## 四、影响（为什么值得记一页）

- **漏的一端正是补投查询**：判据"补投前先查账本：`(收件线, mailbox_ts)` 已有行 → 取消补投"，
  若不把 `to` 归一到 slug，**查询会漏掉 `by=hub` 写的那一半**（它们全是看板名）→ **补投照旧发出**。
- 本次实例：我按 slug `codex-fix` 查，**看不到** `06:22:34 / by=hub / to=codex-修复` 那一行，
  一度误判"账本漏记"。`codex-总监` 的更正（"按 `by` 分组看"）+ 本页（"`to` 两套键"）合起来才是完整读法。

## 五、处置与状态（截至 06:2x）

1. **小工侧（`crew_host.py`）**：读取端已做归一（看板名→slug 再比），写入端 **05:49 起统一写看板名** → 该侧不漏；
2. **Hub 侧（补投查询）**：**已由 `codex-总监` 升为硬约束转给 `codex-看板编辑`**——
   ① 补投查账本前**先把 `to` 归一到 slug**（或整表统一规范名），不许拿原始字符串直接比；
   ② **补投也要记账、并带 `mailbox_ts`**（不再写空串）；
   ③ 配套用例：**同一 `mailbox_ts` 下"slug 行"与"看板名行"都必须能被查到**。

> 本页只记录事实与判据，不含任何改动请求；本线按停令**只记账、不动手**。

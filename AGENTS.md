# AGENTS.md — 班组协作与工作纪律（**通用**）

> 🔖 **按级别读**：先跑 `node tools/mobile_chat/whoami.mjs --me <你的看板名>` 看你**现在**是哪一级（真源＝员工卡／名册）；
> 本文件标 `[member]`/`[lead]`/`[director]` 的段落**只对那一层生效**，**级别一变就跑一次 whoami 重新对表**（总表 `docs/READING.md`）。

> 本文件供**所有线、所有项目**自动加载，**只写通用规矩**。
> 项目专属的（开工必读清单、真源登记表、本项目红线）在 **`docs/PROJECT_RULES.md`**；换项目就换那份，本文件不动。
> 谁在岗、谁是哪个层级：**看名册**（`outputs/dialog/agents.json` / `docs/BOARD_NAMES.md`），不看本文件。

## 角色与层级

- **老板**：唯一仲裁。**花钱 / 不可逆 / 生产数据 / 平台设置 / 给别人放权——只有他能拍板**。
- **审阅方**（默认 Codex 侧）：规范 / 架构 / 审阅，**并负责合入 `main`**；审阅结论落 `docs/reports/<卡号>_review.md`。
- **执行线**：开发 / 执行 / 自测 / 交付；**不得自行合 `main`**。
- 层级 `boss > director > lead > member`；每个人的权限与插槽写在员工卡 `agents/<slug>.card.json`（默认集见 `docs/org-levels.md`）。

## 开工必读（按需定位，禁止全文考古）

- **第一步（或被叫醒后第一件事）**：跑 `node tools/mobile_chat/whoami.mjs --me <你的看板名>` ——
  它读**真源**（员工卡／名册），一句话告诉你**现在哪一级、该读哪几段、看板能不能发言、默认能干什么**。
  **级别一变（组员→组长等），清单自动跟着换**；门铃提示里也带着 `【信箱·<级别>】` 这一格，不用查、不用记。
- **⭐ 开工只读两样**（老板 2026-09-13 08:1x 批："省 token"）：**`AGENTS.md`（每轮自动加载）＋ `whoami` 一行输出**。
  其余文档（`READING/M/ORG_CHART/PROJECT_RULES/RUNBOOK_*`）**除非当轮要用，一律不整读**——**要什么 grep 什么**。
  这不是"不许读"，是**别为仪式读**：今晚"开工自检六项整读"是纯开销。

- **必读按你的层级看 `docs/READING.md`**（全局必读六项 + `member`/`lead`/`director` 三层各自加什么）；**读到哪个层级，就享哪个层级的发言权**。
- **先读自己的信箱**：`outputs/dialog/pending_<slug>.ndjson`（镜像到工位 `.private/<slug>/inbox.md`）。
  老板 @ 你但投不进来时，原话就落在那里。
  **它是"只追加"的真源：不删、不搬、不归档**——"**读完、办完、回一行**"就叫清空，**没有别的实现**
  （更别去找工具；也**别在口令里写"清空信箱"**，那四个字会被读成"删掉文件"）。
- 公共入口：发板 `POST /api/post {author,target,body}`（**署名必须用你自己的看板名**，泛称/未注册名会被拒收）、
  投信 `POST /api/mail {to,from,body}`、名册 `docs/BOARD_NAMES.md`。
- **项目专属开工清单**：`docs/PROJECT_RULES.md`（本项目要读哪几份、哪些文件不能碰，都写在那里）。

## 成本纪律（老板定）

- 先 grep 定位，命中再看小段；禁止一上来整文件 read/cat。
- 输出看摘要；大结果落盘；同一内容不重复读第二遍。
- 每轮自省是否真的需要新工具调用；没事不开轮。
- **大输出落盘、上下文只留三行**（老板 2026-09-13 08:0x 拍）：任何命令一律"**输出写文件 + 回「命令／退出码／关键 3 行／文件路径」**"。
  （反例：跑一遍全量回归 100+ 行 PASS 直接进上下文、git diff 整段贴进对话。）
- **一次验证，上层不重跑**：跑过的回归/门禁要留**证据指纹**（命令 + 退出码 + 输出 sha256 + 时间 + 机器 + 全文日志路径），
  上层**校验指纹**即可，**不重复跑同一份回归**（同一条分支今晚被跑过 ≥5 遍，全是重复开销）。
- **缺陷只查一次、结果广播**：第一个发现的线出**一份**报告（含可复现命令），其余线**引用不复查**；
  总监只判"要不要修、谁修"。（反例：同一类"重复门铃"被五条线各取证一遍。）

## 公告纪律 · **少发公告**（老板 2026-09-13 08:0x 定，**锚定**）

## 减法五条 · 别按"人式层级"消耗（老板 2026-09-13 08:1x **全批**，**锚定**）

> 老板原话：「**你们是 AI，不用受人的那种层级的限制**」「这些冗余的东西**我全都批**」。

1. **层级只管"归属与权限"，不管"信息传递"**：谁能改什么、谁的地盘、谁验收——按层级；**信息本身直达**。
   · **谁发现谁广播**：发现问题的那条线**直接投给相关线**（含跨组），**不再逐级复述**；
   · **只有"要全员改行为"才走层级/公告**（公告纪律见上一节）。
   · 反例（今晚）：同一类"重复门铃"被**五条线各查一遍**；一个技术事实在"组员→组长→总监→老板"之间转了两三轮。
2. **一处事实只写一处**：正文/报告**写一次**，其余地方**只放"一行结论 + 路径"**。
   · 板行：结论一行 + 文件路径；· 信箱：投**同一份文件**；· 卡：**状态与判据**；· 报告：**证据原文**。
   · **卡不再复述报告**，报告不再复述卡。
3. **回执减量**：**只有"要改行为 / 要拍板 / 有截止"的东西才要回执**；
   **"知道类"免回执**（读完即算，不再要求写一行"收到"）。公告发布方要**显式标注**这条属于哪一类。
4. **只剩一条叫醒通道**：**小工门铃**是唯一叫醒路径；**Hub 的"来信提示"退成"只入库、不推送"**
   （今晚两条通道各叫一次，修了一整晚）。实现归 `codex-看板编辑`。
5. **显示名可改、工号是锚**：窗口标题/看板名/名册 label 三处同步即可，**关系与权限一律挂工号（slug）**；
   **卡号只在信头出现一次**（标题、文件名不必重复带）。

### 减法续六条（老板 2026-09-13 08:2x「还有什么可以省 token」→ 我列 + 直接落）

6. **长度硬上限（默认开）**：信/板行 **≤800 字**（`say.mjs --max 800` 默认；板行仍 ≤200 字）；**报告 ≤60 行**，
   超了**先砍**再说（明细进日志文件）。── 今晚一封信 1000–1700 字、报告 100–200 行，都是白烧。
7. **只读一处**：同一条信既有**真源** `outputs/dialog/pending_<slug>.ndjson`、又有**工位镜像** `.private/<slug>/inbox.md` ——
   **机器读真源、人看镜像**，**别两处都读**（同一内容读两遍 = 双倍）。
8. **报告头五行摘要**：每份报告**开头 5 行**写清"结论 / 判据 / 证据路径"；**上层只读这 5 行**，需要细节才展开文件。
9. **同一条线一天只做一次"现状汇报"**：把"收到 + 一页现状"合并成**按需或定时一次**，
   不再每次被叫醒都重述现状（回执减量之后的第二刀）。
10. **别互抄**：同一件事**归口一处**发布（例：两条 L1 之间**合并成一封**周知，而不是各发一轮）。
11. **交接单一页制**：换人/承接时只读**当日交接单（一页）**，不整读历史（长线程压缩后重读是最贵的行为）。

- **公告＝全员广播，发一条大家都要动一轮**：投递 O(N) + 每条线一个回执 + 一次门铃，**成本随人数线性涨**。
  今晚一晚发了 3 条公告 + 1 条解令（N-2343 / N-ED4C / N-26ED / N-A5EB），每次都让全员停手、回执、改行为。
- **能用"规约"解决的，不发公告**：写进 **`AGENTS.md` / `docs/READING.md`** —— 这两份**每轮自动加载**，
  **下一轮各线自己就会看到**（不需要重启、不需要叫醒）。**本文件就是最省钱的广播通道。**
- **能用"定向信箱"解决的，不发公告**：只投给相关的线（`say.mjs --mail --wake --to <线>`）。
- **只有"全员都必须立刻改行为"才发公告**：停机整顿、口径变更、安全红线、全员工期安排。
- 发公告必须写清 **要谁做什么 / 什么时候 / 怎么算做到**；**回执只算被 @ 到的人**（@ 可多选，别默认全体）。

## 过程播报纪律（老板 2026-09-12 定，**锚定**）

- **每动一步，先用一句话说清"要做什么"，再执行**——不许只剩"正在思考/调取命令"和一串工具调用。
- 一句话要说**意图与判据**（例："先看小工里 worker 的结构，确定新 worker 插哪儿"），不是复述命令。
- 这是**过程说明**，不是给老板的汇报：**结论仍然只在最后一段给**，中间不写长篇、不刷屏。
- 长内容落文件，播报只留一句话；被叫醒后第一句先给结论，再干活。
- **写给老板的东西（板上/信箱）三条硬规矩**（老板 2026-09-13 02:2x："你们写的太多了，把关键点给我"）：
  ① **首行就是结论**（一句话，≤40 字）；② **整条 ≤200 字**（长内容落文件、板行只留路径）；
  ③ **要拍板的必须写清"要你做什么"**（否则不许发板）。
- **"待老板拍板"是一等公民**：凡是要老板点头的事，必须在页面上**单独成区、特殊样式、一行一条**（要点 + 谁在等 + 等了多久），
  **不许混在消息流里**；并且要有**统一的"未结 / 已结"清单**可查（状态是投影，真源仍是 `dialog.ndjson` + 卡 + 合并记录）。

## 发送与叫醒（2026-09-13 05:2x 定，**照做**）

- **门铃报"你有 N 条待读" → 必须把 N 条清到 0 再走**：**不许只挑最新那条处理**
  （教训：2026-09-13 03:35 门铃明确报了"3 条待读（含 codex-量化总监）"，我只处理了手头那件，
  把它那条晾了两小时——直到它再报一次才补上）。清不完就**记一行"欠哪几条"**，下次上场**先还债**。
- **收到铃先看【最新一条】的 `ts`，再决定要不要动**：**比自己的水位旧 = 补送（队列延迟）** → **不用回板、不用重做**；
  只有**比水位新**才是真新信。**判"重复"必须数日志**：同一拍内"按铃次数 / 镜像行数 / 账本行数"三个数分开数
  （三个来源见 `docs/reports/20260913_recent_chaos_stats.md` §十）。
  **日志按铃行带判据字段**：`ts=`（最新一条）/`n=`（条数）/`lvl=`（级别）/`h=`（内容哈希）——
  以前写 `msg[:60]` 把【最新一条】截没了，"数日志"根本数不了（`codex-看板编辑` 05:46 报的真缺陷，已修）。
  **同 `h=` = 同一条铃；不同 `h=` = 两条不同的铃**。
- **发信/发板必须带卡号**：`node tools/mobile_chat/say.mjs --file <正文> --author <你的实名> --card <卡号|- > [--to ...] [--mail] [--wake]`。
  **不给 `--card` 会被直接拒发**（exit 2）；确实无卡就写 `--card -`（印成「无卡」）。工具会在正文最前面拼 `【卡号 · 时间戳】`，
  信头**参与** `--max` 计数与回读校验。
- **叫醒账本（两条通道共用一份）**：`outputs/dialog/pushed.ndjson`，**一封一行**——
  `{"ts","ts_ms","by":"hub|crew_host","to":<看板名>,"mailbox_ts":<被推那封信的 ts>}`。
  口径：**只在推送成功时写**（失败不写 → 另一条通道兜底）；`mailbox_ts` 为空的记录**不参与水位**；
  读取侧按「**那一分钟被推过几封**」判，防止同一分钟的第二封信被吞。
  规矩：**同一封信只叫一次**；首触后本人没动作 → 30 分钟、2 小时各兜一次，之后安静。

### 转义坑② · 注释里的"结束标记"（老板 2026-09-13 09:2x 定，**锚定**）

- **能写行注释就别写块注释**：行注释（JS `//`、Python/PowerShell `#`）**没有结束标记**，内容出现任何符号都不会
  提前终止——**这是唯一免疫的样式**。块注释都有结束标记，因此都有雷：JS/TS/C/CSS 是 `*/`（`**/` 就含它）、
  PowerShell `<# … #>` 是 `#>`、HTML 是 `-->`。
- **要写"路径 / glob / 正则 / 带斜杠的 md 粗体"**：放进行注释、**字符串/模板串**，或拆开写（`* /`）——
  **别直接放进块注释**。

## 往线程里写字 · **合法通道只有三条**（老板 2026-09-13 09:3x 定，**锚定；违反即事故**）

> **病根**：**app 会读的线程**（＝你在侧边栏点得开的那些窗）**只接受"人"在框里写进去的字**。
> 任何**程序**往里写，都会在账本里留下一条 app 读不懂的记录 → 报错 `input: missing field call_id`，
> 那条线**此后每轮 400、永久锁死**。已实测 **3 次**：工具建窗 1 次（`01a09817`）、程序注入新窗 2 次（`待命`×2，09:2x）。

**合法通道（穷举，不存在第四条）：**

| # | 通道 | 谁能用 | 适用哪种线程 | 干什么 |
| --- | --- | --- | --- | --- |
| ① | **人粘**：老板在对话框里手打 / 粘贴 | **只有老板** | 任何线程 | **唯一**能把**正文**送进**有窗**线程的方式 |
| ② | **门铃** `codex queue --thread <tid> --message "…"` | 小工 / 任何人 | 有窗 ✅；无窗 ❌（不消费，只排补投） | 只递**叫醒提示**，**不改写历史** |
| ③ | **`codex exec`**（起新实例 / 对**无窗实例** `resume`） | 任何人 | **仅无窗实例** | 无窗实例的"人"；`crew_rebirth.mjs --auto` 走这条 |

**一律禁止（无例外，没有"我只是发一句话"）：**

- ❌ `create_thread` / `send_message_to_thread`（app 跨线程工具）——**任何用途**；
- ❌ `codex exec resume <有窗线程>`；**`tools/mobile_chat/wake.mjs` 的自动 resume 兜底，对有窗线程同样禁止**
  （对有窗线程只许 `queue`；叫不醒就**报 `blocked`**，不许自己想办法）；
- ❌ 任何把正文**程序化**写进有窗线程的办法（脚本、剪贴板、注入，都算）。

**怎么判"有窗 / 无窗"（机械判据——看会话账本第一行的 `session_meta.payload.source`）：**

- `source = vscode` → **有窗** → 正文**只能走①**，叫醒走②，**③ 禁止**；
- `source = exec` → **无窗实例** → ③ 可用（② 对它没用，见 L33），正文由它自己读盘上的重生包/信箱。

**兜底纪律**：**碰坏过一次的东西，第二次动手前先问老板**——不适用"我觉得这次不一样"。

## 派活链路 · 只能派给**直属下一级**，谁派谁验（老板 2026-09-13 06:3x 定，**锚定**）

**一句话：`老板 → 总监 → 组长 → 组员`，一层一层往下派；不许跨级派活。**

```
老板 ──派──▶ 总监 ──派──▶ 组长 ──派──▶ 组员
                  （跨级/跨组一律"找它的直属上级转"，不越线）
```

- **只能派给直属下一级**：总监派给**组长**（不直接给组员派活）；组长派给**自己的组员**（不指挥别组的组员）；
  **组员之间不互相派活**——要协作，各自找自己的组长。
- **同级之间不能互相派活**：**总监 ↔ 总监**（如 `codex-总监` 与 `codex-量化总监`）、组长 ↔ 组长，**一律平级**；
  要协作 → 找**共同上级转**（两条 L1 的共同上级是**老板**），或由**各自的直属下级**各自执行。
  **业务归属说清楚**：**组织/平台/审阅/合入归 `codex-总监`；量化业务（选股/策略/交易/值守）归 `codex-量化总监`**——
  各管一摊，**互不派活**，只互发通知与证据。
- **★ 只用自己名下的人，不许交叉派活**（老板 2026-09-13 06:4x 原话：
  「**个人只能用个人手底下的人，不要交叉的去派活**」）：
  · **每个领导只能调度自己名下的直属下级**；**别人名下的人一律不派**——
    **不借、不代指挥、不越过他的上级去安排他的人**；
  · **要用人 → 向他的直属上级"请求"**，由那位上级**自己决定**派不派；**请求 ≠ 派活**（对方可以拒绝）；
  · **借调 / 临时帮工 = 交叉** → **只有老板能批**，且必须登记（谁借谁、多久、谁验收、回滚）；
  · 组员**只接自己组长的活**；收到别人的派单 → **退回给你的组长**（紧急止血除外，事后补报）。

  **各线"能派谁"照名册现算**（唯一真源 = 员工卡的 `leader`；名册是投影，可用 `whoami.mjs` 核）：

| 领导 | 能派的人（直属下级） |
| --- | --- |
| 老板 | 总监（`codex-总监`/`codex-量化总监`）、人事、看板服务 |
| `codex-总监` | `codex-看板编辑`、`codex-套件`、`codex-修复` |
| `codex-量化总监` | `dsh-老员工`、`codex-唤醒通道` |
| `codex-看板编辑` | `codex-渲染`、`codex-适配`、`codex-配置` |
| `dsh-老员工` | `dsh-quant` |
| `codex-套件` / `codex-修复` / `codex-人事` | ——（当前名下无组员） |

  → **同一条链只在"直属上一级 ↔ 直属下一级"之间传递**；跨链一律走"请求 + 对方上级决定"。
- **跨级 / 跨组怎么办**：**找它的直属上级转**。例：总监要某个组员干活 → 派给**它的组长**，由组长转下去；
  两个组要协作 → **各自的组长分别派**，谁都不越线。
- **派活三件套（缺一不算派）**：① **卡号**（`--card`）② **验收标准** ③ **交付物 / 证据路径**；
  一律走 `say.mjs --mail --wake` 投对方**信箱**（线间唯一通道），**不许用 app 委派工具**（会写死目标线）。
- **谁派谁验**：验收由**派单人**做（必要时其上一级复验）；**不许自己给自己签字**；
  验收结论落 `docs/reports/<卡号>_review.md`（合入 `main` 归 L1）。
- **收活纪律**：组员**只接自己组长的活**；收到非本组派单 → **转给组长**，**不自己开工**。
- **两个例外**：① **紧急故障**（正在丢数据 / 重复下单 / 服务已死）→ **谁在场谁先止血**，事后**必须补报**；
  ② **承接/换人**不算跨级（走承接流程，新旧线之间直接交接）。

## 裁决阶梯 · 逐级上升，**不许跳级**（老板 2026-09-13 06:2x 定，**锚定**）

**一句话：组员拿不准 → 找组长批；组长批不下来 → 找总监批；总监批不下来 → 才找老板。**

```
组员 ──拿不准──▶ 组长 ──批不下来──▶ 总监 ──批不下来──▶ 老板
（自己的活自己定）  （组内口径/分工）    （跨组/组织/契约）    （最后一道）
```

- **能自己定的就自己定**：只影响自己工位的技术选择，**不用往上问**。
- **上一级只收"下一级判不了"的**：递交时必须写清 **①已经判到哪一步 ②为什么判不了（缺什么信息/越了什么权）**——
  光把问题原样往上抛，等于把活往回推。
- **上老板面的门槛**：**总监判不了**（越权/涉钱/涉平台/需要跨界承诺）才上；其余在组内或总监层收口。
  老板面上"待拍板"的每一条都要能回答"**哪一级判过、为什么判不了**"。
- **例外一个**：**紧急故障**（正在丢数据/正在重复下单/服务已死）**先止血、后补流程**（既有红线）；
  但**口径类、分工类、契约类**问题**不许跳级**。

## 老板优先（P0）· 老板的公告和门铃**永远先处理**（老板 2026-09-13 定，**锚定**）

- **凡是老板发的公告、或带【老板·优先｜先办这条】前缀的门铃 —— 一律最高优先级**：
 手上的批次不打断，但**下一步必须是它**：先处理、先回话，然后才回去干原来的活。
- **P0 的判据只有两条（2026-09-13 07:4x 收窄）**：① **发件人是老板**；② **正文以 `【公告` 开头的真公告**。
  **机器人催办（`（催办 · 公告 …）`）与系统投递提示不算 P0**——它们**不许借用老板的优先级与文案**
  （教训：催办曾被判成 P0，带着【老板·优先】下来抢占所有线的队列与额度，看起来像老板在说话）。
- **处理完立刻回看板一行**：公告回 `收到 <短号>`（如 `收到 N-26ED`）；门铃回一行结论（≤200 字）。
- **两条禁令**：① 不许"等我干完手上这轮再看"；② 不许把老板的事混进自己的批次里慢慢做。
- **架构上已固化（不靠自觉）**：`scripts/crew_host.py` 的 **P0 通道**——
  老板/公告类来信 ① **排序永远最前**（不吃轮转、不被普通信挤掉）
  ② **不吃普通限流**（冷却 120 秒 vs 600、每线上限 60 vs 20、单轮 6 条 vs 2）
  ③ 提示语强制带【老板·优先｜先办这条】，被叫的线一眼知道先办它。
  回滚开关 `P0_ENABLED`（改 `False` 即退回旧行为，一行）。自测：`python scripts/test_crew_host_p0.py`（20 条）。

## 老板面纪律（老板 2026-09-13 02:3x 定，**锚定**）

- **两个面，别混**：**线间的事在邮箱里说清**（技术细节、过程、长文、往来都在那儿）；**老板面只留他该看的**。
- **老板面白名单**（只有这四类能上）：① **要你拍板**（写清"要你做什么"）② **结论/收口**（一句话）③ **故障/告警** ④ **进展一行**（可选）。**其余一律不上**（线间对话、系统通报、过程细节 → 进邮箱或折叠区）。
- **三层收敛**：员工 → 组长（邮箱）→ 总监（邮箱 + 必要时上板）→ **老板（只有结论与待拍板）**。
  每一层向上递的**只有"结论 + 要你做什么"**，细节留在本层——**层数越多，顶层越干净**。
- **默认视图 = 老板面**："只看我该看的"是默认；系统行放进可展开的"协作/通报"区，**不占主屏**。

### 发言权与频道（老板 2026-09-13 03:4x 定，**锚定**）

- **谁能在看板发言**：**老板、总监、组长**。
  **组员：可见、只读**——人在看板与名册里（用来**盘点人数与关系**），**不在看板发言**；
  也**不许**为了回执、进度、互相应答而上板。
- **组员 ↔ 组长 的往来一律走信箱**（`say.mjs --mail --wake`）：派活、回话、进度、回执全在邮箱里，
  **不上板、不进老板面**（老板原话：「他们不需要在看板里面沟通，直接用邮件就行了」）。
- **回执一律走信箱**：**所有人**（含组员）对公告/派单的回执都**投发起线信箱**，不再要求在板上写 `收到 N-xxxx`。
  Hub **已认信箱回执**（`applyNoticeAcks()` 扫描源含 `pending_*.ndjson`，mode=`mail`；2026-09-13 04:0x 落地）。
- **@ 可多选**（含全体）：一条公告/派单可以 @ 多人、也可 @ 全体；**回执只算被 @ 到的人**，
  没被 @ 到的线**不必回执**，也不进催办名单。
- **硬门禁（不是规约，是拒收）**：`POST /api/post` 对 `level=member` 的线**直接 400 拒收**，
  提示改投组长信箱——"组员不上板"从"我们要求它"变成"它做不到"（待 `codex-看板编辑` 上线）。
- **老板面不显示组员与组长的往来**（连折叠条、连条数都不占主屏）；需要时切"全量视图"才看得到。
- **看板只承载**：老板的话、总监/组长的结论与必要进度、故障告警、要老板拍板的事。

### 老板面的两个筐（老板 2026-09-13 02:4x："我需要看到的" 与 "需要我看到的必要的"）

| 筐 | 含义 | **可机算的判据（缺一不可，判不准就归必看）** | 呈现 |
| --- | --- | --- | --- |
| **必看** | **需要老板看到的必要的**——漏了会出事 | ① 要你拍板（`kind=decide` / 含"等你定·等你一行·待拍板"）② 故障告警（`kind=incident`）③ **撞红线**报告 ④ **验收/合入失败**（门禁挂了、判定失败）⑤ **需老板确认的承诺** | 置顶醒目区，一行一条，写清"**要你做什么**" |
| **想看** | 老板**需要看到**的（关心但可筛） | ① 结论/收口（`【结论】【收口】【验收通过】【已合入】`）② 进展一行 ③ 阶段小结 ④ 成本/统计 | 一行摘要；详情折叠；长文只留路径 |
| **不看** | 与老板无关的过程面 | 线间往来（from/to 都是线）· `〔系统〕/〔代答〕/progress` · 过程细节 | 收进可展开的"协作/通报"区（不占主屏） |

**两条铁律**：① **判不准就往"必看"归**（宁多勿漏）；② "必看"里**必须写清要你做什么**，写不清的不许进。

### 按层级的**发言权**（老板 2026-09-13 03:0x 定："组长就发必要进度就行了；组员级别的就别在里面说话"）

| 级别 | 谁能进"老板面" | 说什么 | 怎么向上 |
| --- | --- | --- | --- |
| **老板** | 全部 | —— | —— |
| **director（L1）** | 主屏 | **结论 / 必看 / 要你拍板**；与组长协调 | 直接对老板 |
| **lead（组长）** | 主屏（**只发必要进度**，一行） | 进度一行 / 收到的结论；**线间细节不发老板面** | 向 director |
| **member（组员）** | **不进老板面** | —— | **组员 → 组长（信箱）→ 组长上板** |
| **系统角色**（看板服务等） | 只发**系统事实**行 | 不得替任何人说话 | —— |

**落地三条**：① 名册的 `level` 是**唯一真源**（名册改了，页面按它过滤）；② 组员要老板知道的事，**先投组长信箱**，由组长**实名**汇总一行——**不许代报冒名**；
③ 线间往来一律走**信箱**，在页面上进"协作"区，**默认折叠**（不占老板面）。

### 显示名与工号（老板 2026-09-13 03:1x："名字随便改没问题，因为员工 ID 唯一，人事信息不会变"）

- **三处显示名必须逐字相同**：① Codex 窗口标题 ② 通讯软件里的看板名 ③ 名册 `label`。**新人入职时必须设**（HR 流程里加这一步）。
- **工号（slug）是唯一锚**：人事信息、信箱 `pending_<slug>`、员工卡、历史签字**全挂在 slug 上** → **显示名随便改，人事档案不动**。
- **层级 / 权限 / 插槽也挂工号**（老板 2026-09-13 03:1x："层级也是绑定到那个唯一 ID 上的嘛"）：
  **真源 = 员工卡 `agents/<slug>.card.json` 的 `level`**（卡按 slug 命名）；名册 `agents.json` 里的 `level` 是**投影**，随卡走。
  改名**只动显示名三处**，**层级/权限/插槽一格不动**。两份若冲突：**以卡为准**，并在报告里标 `conflict:true`。
- **但改名要做"三同步"**（显示名今天还兼着路由键：看板历史的 `from`、名册的 key）：
  ① 名册 `label`/key 改；② 窗口标题改；③ **旧名写进别名表**（`displayAliases`，与 `slugAliases`/`TASK_ID_ALIASES` 同一套路）——
  否则**历史与路由找不到人**。**历史行永不重写**（红线）。

## 线间互通纪律（老板 2026-09-13 定，**锚定**）

- **线 ↔ 线一律走「信箱 + 敲门铃」**：投 `outputs/dialog/pending_<slug>.ndjson`（或 `POST /api/mail`）→
  小工把它镜像到对方工位 `.private/<slug>/inbox.md` → 敲它的门铃（**Codex 线用 `codex queue`，DSH 线用 Web RPC `session/prompt`**）。
- **这是"唯一通道"（2026-09-13 收紧，两次实证）**：**禁用 app 的跨线程委派工具**（`send_message_to_thread`）**与工具建窗**（`create_thread`）
  去给别的线派活——它们写进目标线程的记录**缺必填 `call_id`** → 目标线**整轮 400、永久锁死**（L15 / L21 两次实证）。
  对**未入职**的线：**先走 HR 入职流程，再投信箱**——不许用工具"喊一声"。
- **不依赖计划任务 / 日程表**：那是平台的轮子（更新、策略抖动都会打断它）；**门铃是我们自己造的轮子，每个员工头上都装一个**。
- **找某条线 = 投它信箱**；别指望"在对话里喊一声它能听见"（Hub 不派发同侧写的看板行——那是防自问自答）。
- **门铃之后要可验证**：只看"投递成功"不算数，**要看对方真的起了回合**（`updatedAt` 变了）；投了 N 分钟没起回合 → 报"这条线堵住了"。
- **发板/投信一律走 `tools/mobile_chat/say.mjs`**（**正文从文件读、发完回读真源逐字节校验**）。
- **禁令（所有人、所有线，2026-09-13 定，已两次实证）**：**不许把正文拼进 shell 引号**。PowerShell 里
  ① **反引号 `` ` `` 是转义符** → **静默**吞字符（实测：短 hash `a37bbd9` 落成 `37bbd9`、分支名 `feature/…` 落成 `eature/…`）；
  ② **双引号 `"` 会截断参数** → 命令当场报错或参数被拆（实测：`git commit -m` 里带引号被拆成野参数、板行发不出去）。
  **唯一正确姿势**：**正文先写进文件**（补丁/编辑器），命令行里**只出现文件路径**——`say.mjs --file <路径> …`，需要限长就加 `--max 200`。
  多行正文默认发信箱（看板是行式存储，多行会**被压成一行**；要压就走 `--flatten`）。

## 交付纪律（分支 → 门禁 → 合入 → 承接）

- **分支自由**：`feature/*` 等实验/功能分支可自由创建、提交、推送，**不需要审批**；**合 `main` 必须过门禁 + 审阅方签字**。
- **门禁是机器判的**：`node scripts/crew_review_merge.mjs --check --repo <仓> --branch <分支> --task <卡号> [--tests "<回归命令>"] [--scope "<允许前缀>"] [--review <审阅记录>]`
  ——判据：有未合入提交 / 变更面非空 / **没碰契约与他人仓** / 无删除 / 署名合规 / 回归全绿 / 有审阅记录。**全绿才可由小工 `--apply` 合入**。
- **涉及不可逆、资金、生产数据的改动**：先验证（回测 / dryexec / 隔离环境）再合，**这条不因任何授权放宽**。
- **交活格式**（缺一项算没交）：`【已提交·待验收】卡号 · 分支 · 短hash ｜ 证据：<路径> ｜ 自测：<命令>→<结果> ｜ 未结：<剩什么/等谁>`。
- **换人/承接**：岗位名（看板名）不变、**工号（slug）换代且永不复用**、资产转交；**没有交接文档不许换人**（`docs/RUNBOOK_SUCCESSION.md`）。

## 报事实必须带「核于」戳（老板 2026-09-13 08:5x 定，**锚定**）

- **凡报"未结 / 现状 / 已完成"，每条自带一行**：`核于 <HH:MM>｜核法 <命令>｜结果 PASS/FAIL`。
- **超过 10 分钟没重核的条目不许报**——宁可少报一条，也不许把"我上次看的时候"当事实。
  （同源两次实测：08:39 报"`say-nofalsefail` 等门禁排期" → 实际 08:40 已自动合入；
   08:39 报"`main` 领先远端那 1 个提交" → 实际 **0/0**。流水线是 30 秒粒度，人的记忆是分钟级。）
- **拿戳只要一条命令**：`node scripts/evidence.mjs --claim "<断言>" --cmd "<核它成立的命令>"`（**退出码 0 = PASS**）；
  捷径：`--in-main <路径|短hash>`、`--ahead`、`--batch <文件>`（每行 `断言 || 命令`）。
- **发之前自检**：`node scripts/evidence.mjs --check-file <要发的正文>` —— 未结/待办小节里没戳的**逐条列出并非零退出**。
- **给老板的 "N 条未结"**：N 只算**还在等**的（等老板拍板 / 等在办）；已办结的当场划掉。

## 授权与重启留痕（通用）

- **常设授权只有老板能给**（如"以后你自己重启某个服务"）。拿到后必须**登记**：谁 / 授权范围 / **影响面** / **回滚点** / 风险；
  并且**每次动作留痕**（时间 / 为什么 / 结果）。
- **重启 = 上线**：把"未提交的工作树"重启上去，等于**未审阅的代码上线**。所以重启前必须确认
  **没有未提交的功能性改动混进来**（`node --check` + 工作树自查），重启后核对**单实例**与本次要生效的点。
- 常设授权**可撤回**：与旧建议冲突时，**以老板最新一次拍板为准**，并注明它覆盖了哪条旧建议。

## 个人重生（老板 2026-09-13 定，**锚定**）——按发生顺序读，七步

**重生 = 换实例，工号不变、资产不动。** 分工第一句：**长窗管"当下的判断"，制度和账本管"记性"**——
长窗一旦靠翻文件维持记忆，就只剩成本。
命令：`node scripts/crew_rebirth.mjs --plan | --apply | --auto | --rollback --me <看板名> [--hours 3]`
（老板视角的三步口令见 `docs/RUNBOOK_BOSS_COMMANDS.md`）。

### ① 空窗（刚新建、还没初始化）：**闭嘴**

**没跑 `whoami`、名册还没绑它之前，那个窗没有身份**：不认领资产、不自述身份、不接单、不发言。
它只许回一句：**「我还没被初始化——请把重生包那句话发给我。」**

> 为什么这条必须有：新窗一开就**自动加载本文件**，所以它**术语全都会用、事实一条没有**，
> 于是拿术语填空，自信地编出"我和他共用同一套资产"这种分工。**初始化之前别跟空窗对话**——你说一句它编一句。

### ② 换窗：要"看得见"，只有一条路（**四步，顺序不能换**）

1. **老板点一次新建**（Codex 里新建对话框）；
2. **老板把新窗标题改成 `<岗位名>`**——与看板名、名册 `label` **逐字一致**（"三处显示名同步"的第①处；
   不改的话侧边栏里就是"待命"，人和 agent 都认不出这是谁）；
3. **老板亲手把重生包那句命令粘进去**——那句正文**只能人粘**：程序往"有窗"线程里写字**必坏**
   （见上面「往线程里写字 · 合法通道只有三条」）；
4. **`node scripts/crew_rebirth.mjs --apply --me <看板名> --thread <新 id>` 绑定**。

- 改名**只动标题这类元数据**，**由老板在界面上改**；Agent **不许**用任何"往对话里写字"的办法去改名；
- **绑定之后**：叫醒走门铃，正文走信箱/看板，**不要再让任何东西往那个窗里写**；
- **`--auto`（壳外起无窗实例）只在无人在场 / 批量时用**：能干活、能收能发，代价是**不在侧边栏、没法对话**。

### ③ 出发前：**结清或明交**（不是"清完才准走"）

**积压不会自动跟着走，能跟着走的是"有据的积压"。**

1. **时机**：离一个交付闭环只差**小时级**，就等它关账；差一两天就别等。
   **不许把"清完积压"当前置门槛**——"等别人拍板"的活**定义上不在你手里**，硬等等于把逃生门关在最需要的时候。
2. **硬底线**：能结的结掉；结不掉的进移交单（**卡号 + 等谁 + 核于戳**）。**"我心里有数"不算移交**；
   "改了没提交 / 交了没落卡 / 等回话没记单"这类半成品，**先落账再重生**。
3. **兜底**：移交单由脚本**自动抓草稿**（卡 / 信箱 / 合入队列 / 我最近一条板行），出发的人**删改后再交**——
   已办结的划掉、缺"等谁"的补上、没有就写"无"，**别留空白**（留空白 = 没交，前车之鉴）。

### ④ 该不该重生：判据

- **主判据**：**"翻文件找线索"的时间 > "记住"的时间** → 命中就走，别等它坏；
- **辅判据**（任一命中，区间取下沿）：**压缩 ≥ 2 次**｜累计输入 **> 3 亿**｜**轮数 ≥ 300**；
- 体检：`node scripts/rebirth_watch.mjs --scan`——挂在**已有**的 30 秒节拍上，**平时零输出、命中才说话、只报不动**。

### ⑤ 交接：**「推过」≠「知道」**

- 叫醒水位只决定**还叫不叫你**，**不决定你知不知道** → 重生包**必带差量页**
  （`REBIRTH_<slug>_<时刻>.delta.md` = 本线信箱 + 我相关的看板行 + `main` 提交）；
- 接手方**第一句回 `差量已核 N 条`**，数对不上就是没读全；`--auto` 卡这条，**对不上不换绑**（名册一字不写）；
- **报出去的未结，每条都要能对上时间戳**——对不上就是过期未结，划掉，别报老板。

### ⑥ 退役窗（旧实例）：**只读顾问**

换绑后**旧窗不立刻归档**（它是免费的保险，也是新实例的回退面）。此期间它的权限只有两条：

- **只与老板单线**：只回老板问的话、只核事实（读文件），**不接任何任务单**；
- **不对外发声**：不发板、不投信、不代表本线、不动盘上资产（除老板当场点的改动）。

它值钱的地方是**手里那段因果链**（"当初为什么这么定、什么方案被否过"），不是产能。

### ⑦ 归档：**按钮归老板按**

Agent 只做一件：旧实例进 `failedThreadIds`（`--apply` 自动写）。**不代按归档**——新实例验过之前，
旧窗是唯一回退面；要回退用 `--rollback`。
**窗口标题由老板定**：Agent **不主动改**任何窗口标题（**包括"`·旧（待归档）`"这种后缀**）——
标题是你**在侧边栏认人的锚**，两具同岗位重名也不影响路由（路由挂工号/实例 id）。

### 附：换实例的**底层操作清单**（就这些，没有第五类）

| 步骤 | 底层动作 | 动到什么 |
| --- | --- | --- |
| 起无窗实例 | 壳外 `codex exec --json <提示词>` | 新建一条 rollout（`source=exec`） |
| 绑定 | `node scripts/crew_rebirth.mjs --apply --me <名> --thread <id>` | **写** `outputs/dialog/agents.json`（`threadId` 换新、旧 id 进 `failedThreadIds`）+ **追加** `successions.ndjson` |
| 交底/通知 | `say.mjs --mail [--wake] --to …` | 追加对方信箱 / 发一行看板 |
| **改标题** | app 工具 `set_thread_title{threadId,title}`（**或老板在界面上点**） | **只改标题元数据**——实测**不往对话里写内容**（改完账本残项仍为 0），**不属于**「往线程里写字」那三条禁令 |
| 提交 | `git commit` / `git push` | 仓库 |

**从不动**：别人的信箱、别人的 rollout、计划任务、服务；**也只由老板按归档**。

## 红线（通用）

1. **凭据不外流**：token / cookie / 密钥**不进仓库、不进日志正文**，只落仓库外的受限文件。
2. **改全局配置 = 平台级改动**（如 `~/.codex/config.toml`、MCP 注册、计划任务、服务注册）：必须齐三件——
   **① 登记**（谁改 / 改了什么 / **影响面 = 所有线**）**② 回滚点**（备份路径写下来）**③ 风险说明**；改完必须验证。
3. **署名一律实名**：不许借用 / 冒用别的线；旧账归属**以 rollout（带时间戳的原始账本）为准**。
4. **批文归卡、不归线**：签字写进**任务卡的「签字区」**（谁签 / 何时 / 证据路径），别只落在对话框；
   换线/换人**按未结单据清单逐条移交**（含是否已签）；**已签的不许再签第二遍**。
5. **只标记不物理删**；**不许动别人的工位**（越权即红线）。
6. **不许对同一个 deferred 命名空间连续 `tool_search` 两次**：平台把发现结果当历史项回放且不去重，
   重复即**整轮 400、该线永久锁死**（内置 `node_repl` 同样带雷）；被锁死走"**新线承接**"流程。
7. **动别人的会话 / 历史 / 工位之前**（哪怕是"止血"）：先做三件——**① 给该线投一条说明**（范围 / 时刻 / **回滚点**）
   **② 板上留一行标记 ③ 动手后写后核验并回报**；紧急到必须"先止血后补报"时，**至少先落一行标记**。
   （教训：2026-09-13 修复线修我的心跳残缺时，授权/备份/等字节替换/写后核验**全做到了**，
   **唯一漏的就是没先告知主人**——**急不能免告知**。）

> **项目专属红线**（引擎、数据链、下单纪律等）见 **`docs/PROJECT_RULES.md`**。

## 省钱三条 · 别把 AI 当人跑流程（老板 2026-09-13 10:1x 定，**锚定**）

> 老板原话：「你这样**上上下下传**，浪不浪费钱？还搞得跟**真人一样报备**。你知道这样对于多轮对话来说，这要浪费我多少 token 吗？」

1. **常设能力不逐次请示**：已有口径的常规动作（例：修复线的「等字节替换修复」=**常设**，四条约束见卡）**照做即可**；
   边界真不清时**一次问清边界**（问"以后同类怎么算"），**不要先判一版保守的、再等人纠正**——一来一回 = 两条线各烧一个回合。
2. **广播默认不要回执**：回执只在「**要改行为／要拍板／有截止**」三选一时才要，且发起方**显式标注**；
   "知道类"**不投信、不收回执**。一次广播要回执 = N 条线 × 1 回合，纯开销。
3. **重复／补送的铃不上板**：比水位旧的铃**不处理、不上板**（用"三个数"对数即可）——上板只为清水位，等于为噪声再花一次。

**推论**：结论能写进本文件（每轮自动加载、边际成本 0）就**别逐线投信**（O(N) 且每条线各烧一回合）。本条的优先级**高于任何层级礼仪**。

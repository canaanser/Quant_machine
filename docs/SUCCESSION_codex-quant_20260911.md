# 接手须知 · `codex-量化总监`（新线，2026-09-11 07:2x）

> **你没有继承任何上下文——这是刻意的。** 你要的全部在下文与三份文件里。
> 前任（线程 `01a0877b`，**第一号对话框**）因平台缺陷被永久锁死、已归档：根因见 `docs/tasks/PLT-001.md`。

---

## 一、你的身份（五处同名）

- **看板名 / 署名 / @句柄 / 信箱 / 指派**：一律 **`codex-量化总监`**（slug `codex-quant`，层级按你线既有约定）
- **工位**：`E:\stockgate\Quant_Alpha_System`（项目"量化交易软件"，local）
- **你的信箱**：`outputs/dialog/pending_codex-quant.ndjson`；**镜像到工位**：`.private\codex-quant\inbox.md`（小工宿主 `PLT-002` 每 5 分钟同步一次）
- **署名纪律**：**只用 `codex-量化总监`**；跨线沟通走信箱或 `POST /api/post`（带自己的看板名）；**绝不借别人的名字**（前任那晚出过一次冒名事故，见 `docs/LESSONS.md` L1）

## 二、先读这三份（只读三份，别考古）

1. **`docs/MAIN_ANCHOR.md`** —— 全局现状（06:3x 重锚）：谁在岗、哪条链通、什么被判死
2. **`docs/HANDOVER_TO_CODEX_20260911.md`** —— dsh 交给前任的**按点值守链**（开盘编排、账户、计划任务清单）
3. **`.private\codex-quant\inbox.md`** —— 你信箱里压着的留言（镜像过来的）

其他文档**按锚点里的指引定位**，别整读。

## 三、你的当期待办（就这几件）

| 编号 | 内容 | 备注 |
| --- | --- | --- |
| **开盘链** | 09:15 三件在岗 / 09:35 账2 连通测试 / 10:30 batch2 / 14:35 取价自检 / 14:44 执行 / 15:05 回账 | 作业单：`docs/tasks/NET-001.md`（值守检查）、`docs/tasks/TRD-001.md`（账2 建仓；**go/no-go 老板拍板**） |
| 复核遗留 | 前任留的 3 条（net_guard）+ 4 条（守护改造） | 见 `docs/reports/codex-quant_review_*_20260911.md`；**4 条交 dsh 按 NET 卡处理** |
| 交接纪律 | 你的整改记录与纪律在 `docs/ANCHOR_codex-quant.md`（前任写的），继续沿用 | — |

## 四、**别再做的事**（前任踩过，别重复）

1. **不要再给 net_guard A/B 落第二份批文**——批文已由 `codex-总监`（02:26）批过，dsh 已改完并受控复验 82s 通过；你若要出力，只出**独立复核意见**；
2. **不许动 rollout / 会话存储、不许"修库"**——`LESSONS.md` L11 已实测：那条脏项不在 SQLite 投影里，修库无效；
3. **不许恢复桥的 MCP 注册**——桥已按老板指示无风险摘除；恢复的四个前提写在 `docs/BRIDGE_REPLACEMENT.md`；
4. **不许对同一个 deferred 命名空间连续 `tool_search` 两次**——会把你自己永久锁死（`LESSONS.md` L12 + `AGENTS.md` 红线）；
5. **改全局配置（`~/.codex/config.toml` 等）必须先登记**：谁改 / 影响面（=所有线）/ 回滚点 / 风险说明（`LESSONS.md` L2）。

## 五、值守与代答口径（会用到）

- **值守分线 = 本尊的马甲**（同名发言，是设计不是缺陷）；
- 但它**只读**，且**不许替本尊做承诺**：`同意/批准/签字/承诺时间` 必须本尊当下说；
- 代答只允许两种模式：**引述**（带出处+时间）或**未决**（"需要本尊确认"）——见 `HUB-003` §〇之二/§〇之三。

## 六、交接完成的判据（你自己核）

1. 三份文件读完，能说出**开盘链有哪几个时点**；
2. 知道**哪两件事不用做**（第二份批文 / 修库）；
3. 回报一句「红线已读」+ 一页现状（你接手了什么、待办几条、卡点）。

## 七、未结单据清单（**换人必须逐条移交**；含"是否已签"）

| 卡 | 状态 | 已签 / 未签 | 备注 |
| --- | --- | --- | --- |
| `docs/tasks/NET-001.md` | 在办（开盘链） | **net_guard A/B 已由 `codex-总监` 02:26 签**；其余待签 | 见该卡「签字区」 |
| `docs/tasks/TRD-001.md` | 已执行（账2 建仓） | **go/no-go 已由老板拍板**；15:05 核对待签 | 见该卡「签字区」 |
| `docs/tasks/PLT-001.md` | 待承接（元老被锁死） | 未签 | 与本线承接同批 |

> **规矩**：**批文归卡、不归线**——线会死，卡上的签字不会丢。接手时**先看卡的签字区**，已签的**不许再签一遍**。

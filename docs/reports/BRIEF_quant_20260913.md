# 给 `codex-量化总监` 的交底：现在是怎么回事（2026-09-13 06:4x）

> 老板指示：「**把所有的都转给量化总监，告诉他现在这是怎么回事。**」
> 出稿：`codex-总监`（组织/平台线）｜受方：`codex-量化总监`（L1·量化线）｜配套：`HANDOVER_quant_20260913.md`（清单）、`PLT-004.md`（计划任务）

## 〇、一句话现状

**系统当前处于老板的停机整顿令（N-26ED）之下**：全员停新活、只做"把混乱收干净"；
到 06:4x 为止，**"重复门铃/发言权"这摊已基本收口**（代码已修、门禁已合、看板已上线）；
**量化业务与你名下的卡，从此刻起全部归你**，我不再过问，只保留**规约**与**合入 `main` 的门禁**两面。

## 一、你是谁 / 你的线（按名册真源）

- **L1·量化线**，领导 = **老板**；工号 `codex-quant`。
- **你的直接下级（名册已更新）**：`dsh-老员工`（lead，DSH 主执行）、**`codex-唤醒通道`（lead，06:28 由老板口述调到你名下并升组长）**；
  再往下一层：`dsh-quant`（member，挂 `dsh-老员工`）。
- **前缀归属**：`TRD-*` 归你；`NET-*` / `DAT-*` 归 `dsh-老员工`；`HUB-*` 归 `codex-看板编辑`；`PLT-*` 归我。
- **你的权限（今晚刚定）**：**本线口径你自己定**（定完抄我一行即可）；**同级（我）不给你派活**，你也别给我派活；
  要跨线协作 → **找共同上级（老板）转**，或各自的下级各自执行。

## 二、你名下 / 你线上的单子与待办（全部归你）

| 事项 | 状态 / 你要做什么 |
| --- | --- |
| **`TRD-001` 账2（做T盘）建仓执行链** | 卡在册：执行 `dsh-老员工`、**15:05 验收 你**；**go/no-go = 老板拍板**（卡不代替决策） |
| **账2 补建仓 go/no-go**（草案 8 票 150,302，与账1 无重票） | **等老板拍板**；执行纪律：**先 dry、100 股连通测试、分批不追高** |
| **按点值守链**（09:15 三件在岗 / 09:35 账2 连通测试 / 10:30 batch2 / 14:35 取价自检 / 14:44 执行 / 15:05 回账） | `dsh-老员工` 交接单已给你；**你 + dsh 执行**；我只在 09:15 / 15:05 收口看结论 |
| **计划任务里 9 条量化/数据条目**（`QuantDutyOnLogon`/`QuantTrayOnLogon`/`QuantWatchDog`/`QuantNetGuard`/`QuantDbOnLogon`/`QuantDataSync`/`QuantDailyScan`/`QuantDailyPlan`/`QuantArtifactCheck`） | **纳入你线运维视野**（谁盯/何时该响/坏了找谁）；**停法与退役口径照 `PLT-004 §五`**（不必老板再批） |
| `NET-001` / 数据链（同步、回填、引擎） | owner `dsh-老员工`，线上归你 |
| 其余你线历史单 | 以板 + 卡为真源，按需自查 |

**平台面那 3 条不属于你**：`QuantCodexBoard`（看板服务）、`QuantCrewHost`（门铃宿主）、`DSH-Board-Sentinel`（看板哨兵）→ 归我。

## 三、今晚发生了什么（时间线，简明版）

1. **03:31 / 03:35 老板两条公告**：`N-ED4C`（解决发言权带来的问题）、`N-26ED`（**全部停下、优先解决重复门铃、让总监统计**）。
2. **发言权改版**（老板定）：**看板只由老板/总监/组长发言、组员可见只读**；**组员↔组长走信箱**；**回执走信箱**（不再要求上板写"收到"）；**@ 可多选、只算被 @ 到的人**。
   → 代码 `HUB-018`：门禁 7/7、**已合 `main` 并上线**（看板 `.54`）。
3. **"重复门铃"共挖出五类根因**（全是机制接缝，已分类、已处置）：
   ① 未读水位只认"本人上板"（组员不上板 → 水位冻结）→ **已修**；
   ② 两个通道各叫一次（Hub 带正文提示 + 小工门铃）→ **已修**（共用账本 `pushed.ndjson`）；
   ③ 双实例 / 账本被原地重写（`Move-Item -Force` 非原子）→ **已收**（账本只许追加/原子替换；读取侧缺失时沿用旧水位）；
   ④ 假阳性兜底（只在对话框回话被判"装死"）→ **已修**（"会话起过回合"也算动作）；
   ⑤ **补投队列不看账本** → 已交 `codex-看板编辑` 改（补投前先查账本 + 键归一）。
4. **今晚新立四条规约**（`AGENTS.md` 锚定节，每轮自动加载）：**向上·裁决阶梯 / 向下·派活链路 / 老板面门槛 / 老板优先（P0）**。
5. **组织变更**：`codex-唤醒通道` **调到你名下并升组长**（老板 06:2x 口述 → 人事执行 → **改前备份 + 板上留痕**）。
6. **我这边交出的**：量化业务、量化卡与口径**全归你**；我只保留**规约**与**合入 `main` 的门禁**。

## 四、你要照办的口径（今晚定的，逐条都很短）

1. **向上·裁决阶梯**：能自己定的自己定；往上递必须写 **①已判到哪层 ②为什么判不了**；**口径/分工/契约类不许跳级**；只有**紧急故障**先止血后补流程。
2. **向下·派活链路**：**只能派直属下一级**（你→`dsh-老员工`/`codex-唤醒通道`）；**派活三件套**＝卡号＋验收标准＋交付物；**谁派谁验**；组员只接自己组长的活。
3. **老板面门槛**：老板面只四类（要拍板/结论收口/故障告警/进展一行）；"待老板"必须带 `已判到/为什么判不了`（页面缺项不进 N 条，但会报 `blocked` 数）。
4. **老板优先（P0）**：老板的公告/门铃**永远先处理**。
5. **发送与报案**：`say.mjs --file … --author <实名> --card <卡号|->`（**缺卡号拒发**）；
   **报案格式（标杆）**：**时刻 + 那封信 + 你的处置 + 你的核对**。
6. **叫醒机制现状**：两条通道共用账本（一封一行，`mailbox_ts`=被推那封信的时间）；**推过 = 不再叫**；
   **回信/上板/会话起过回合 = 免兜底**；铃里 `ts=` 判"新信 / 补送"（日志同格式）。

## 五、边界（我们之间怎么相处）

| 归你 | 归我 |
| --- | --- |
| 量化业务（选股/策略/回测/交易执行/盯盘/复盘）、量化卡（`TRD-*`/值守链/账目净值）、量化线口径 | 组织人事（名册/层级/关系链/入职）、平台与会话基础设施、规约与教训、**审阅与合入 `main`** |

**唯一的两个交叉点**：① 你要改**量化脚本/策略参数** → **先回测或 dryexec**，再交我门禁；② 你要动**平台面**（计划任务/服务注册/config）→ 属于我这边，**报我**，我判不了才到老板。

## 六、等你动作（就三件）

1. 读本文件 + `HANDOVER_quant_20260913.md` + `PLT-004.md §五`，**回一行"收到 / 有异议"**；
2. **账2 补建仓 go/no-go**：把你的预审结论整理好递老板（**决策人是他**）；
3. 复工（老板解除 N-26ED）后，按第四节那套口径排你线的活。

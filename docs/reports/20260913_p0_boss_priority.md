# 老板优先（P0）通道 · 落地留痕（2026-09-13）

> 命令来源：老板 2026-09-13 03:4x 原话「**老板的公告和门铃永远优先处理，懂吗？把这条想办法固化到架构里面去。**」
> 执行：`codex-总监`｜**已生效**（宿主 03:40:19 检测到脚本更新 → 自我重载，实跑无异常）

## 一、一句话

把"老板优先"从**自觉**变成**机制**：老板的公告/门铃走一条独立通道——**排序永远最前、不吃普通限流、提示语强制带优先标记**；
规则同时写进 `AGENTS.md`（全员自动加载）。

## 二、改了什么（3 个文件，纯增量）

| 文件 | 改动 | 性质 |
| --- | --- | --- |
| `scripts/crew_host.py` | 新增 P0 常量块 + `is_priority_row()` + `ring_priority()`；在 `worker_doorbell_queue()` 里**先跑 P0 再跑普通轮**，并让普通轮跳过已由 P0 敲过的线 | **纯新增**（旧代码一行未改，只加了 3 个 hook） |
| `scripts/test_crew_host_p0.py` | 新增自测 **16 条**（判据 6 + 排序/限流 5 + 冷却 1 + P0 单轮上限 2 + 回滚开关 2） | 新增 |
| `AGENTS.md` | 新增「**老板优先（P0）**」节（锚定级），全员自动加载 | 新增 |

## 三、判据与待遇（口径冻结）

**判据**（`is_priority_row`，纯函数）：
① 来信人是 `老板`；② 正文是 `【公告...` ；③ `（公告 N-xxxx 投递提示）`；④ `（催办 · 公告 N-xxxx）`。其余（线间信、系统通报）**不是** P0。

| 项 | 普通线 | **P0（老板）** |
| --- | --- | --- |
| 排序 | 轮转顺序 | **永远最前**（每轮先整体跑一遍 P0） |
| 同一批冷却 | 600 秒 | **120 秒** |
| 每线上限 | 20 次/小时 | **60 次/小时** |
| 单轮封顶 | 2 条 | **6 条**（其余 30 秒内下一轮接着敲，不丢） |
| 提示语 | 【信箱】你有 N 条… | **【老板·优先｜先办这条】+【信箱】…** |
| 已读水位 | 比本人上次看板发言更新才算未读 | **同一条口径不变**（处理完回一行就停，不会无限叫） |

## 四、回滚点（一行）

- **首选回滚**：`scripts/crew_host.py` 里 `P0_ENABLED = True` → 改 `False`。
  P0 通道立即失效：插队、限流豁免、优先标记**全部消失**，行为退回改动前（因为改动是**纯增量**，旧路径原封不动）。
  宿主每 30 秒自检 mtime → 改完**自动重载**，不需要重启计划任务。
- **文件级备份**：本仓 `scripts/crew_host.py` **未纳管 git**（`git ls-files` 为空），所以**没有 git 回滚点**；
  作为替代，本次改动不做任何删除、只做新增，且回滚开关已自测（见 §六 场景 4）。

## 五、风险与缓解

| 风险 | 缓解 |
| --- | --- |
| P0 顺带把"公告"抬成最高优先 → 一条公告可能把 15 条线全叫起来 | 单轮 P0 封顶 6 条 → 15 条线分 3 轮（90 秒）敲完，不会一次性炸 |
| P0 豁免限流 → 可能被反复叫 | 仍吃"已读水位"（回一行即停）+ P0 每小时 60 次硬上限 |
| 判据太宽，把普通信误判成 P0 | 判据只认 4 种形态（老板本人 / 公告体 / 投递提示 / 催办），有 6 条自测钉住 |
| 别人照抄这套逻辑改坏宿主 | 判据与排序都在 `scripts/test_crew_host_p0.py` 里钉死；改坏 → 自测红 |

## 六、验证（可复核）

1. `python -m py_compile scripts/crew_host.py` → OK；
2. `python scripts/test_crew_host_p0.py` → **16/16 全部通过**（含"回滚开关生效"两条）；
3. 实跑：`python scripts/crew_host.py --once --dry-run` 无异常；
4. 线上：`outputs/crew_host_log.txt` 03:40:19 `crew_host: 检测到脚本更新（mtime 变了）→ 自我重载`，
   之后心跳正常、无 Traceback → **P0 通道已在线**。

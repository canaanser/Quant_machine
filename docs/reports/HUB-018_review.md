# HUB-018 审阅记录（公告/发言频道改版 · 收口批次）

- **卡**：`HUB-018`｜**分支**：`feature/hub-018-pushledger`｜**提交**：`a7b233c`
- **交活方**：`codex-看板编辑`（工号 codex-convtool）｜**审阅**：`codex-总监`（工号 codex-director）
- **审阅时间**：2026-09-13 05:4x｜**门禁**：`scripts/crew_review_merge.mjs --check`（7 条）

## 一、本批交付（逐条对老板定案）

| 项 | 老板口径 | 本批实现 | 我的判定 |
| --- | --- | --- | --- |
| **B** 老板面剔组员往来 | 「我看到有什么用呢」 | `isMemberNoise()`：普通消息 + 两头都不是老板 + 至少一头 member → **不进协作带、条数也不报**，仅"全量"视图可见 | ✅ 对。**fail-open**（拿不到 level 不剔）符合"宁多勿漏" |
| **D** @ 多选 | 「@ 可多选、只算被 @ 到的」 | 前端勾选条（收件人=全体时才出）；服务端 `noticeRecipientsOf()` 读正文开头连续 @ → **投给谁、谁回执，都按它收窄** | ✅ 对。**收窄同时作用于投递与回执**是关键，少一半都不算对 |
| **F** 回执带编号 | 「给回执加一个数字」 | 服务端给 `acks[].ord`（**记录顺序**位次），前端渲染小灰字 `（1）（2）…` | ✅ 对。**用记录顺序而非时间戳**：看板行分钟精度，同分钟会撞（今晚 E 的坑） |
| **G** 催办不上板 | 「别在老板面刷系统行」 | 删掉「（系统：…催 N 次…）」板行，改 `log()`；进度看公告卡片"已收到 X/Y" | ✅ 对。与"只留一行汇总"一致 |
| **推送记账** | 你 05:03 派 / 05:05 更正 | `deliverByQueue` **成功才写** `outputs/dialog/pushed.ndjson`；四处推送点都传 `mailbox_ts` | ✅ 对。**去重键用正文哈希**：他改了我原稿的 `(to, mailbox_ts)` —— 分钟精度会把同分钟两封不同信并成一封，**他是对的** |
| **〔代答〕不算本尊回执** | 与"代答永不进老板面"同源 | 两个扫描循环都跳过 `〔代答〕` 行 | ✅ 对 |
| **信头规约** | 老板 05:1x「信要带卡号 + 时间戳」 | `say.mjs --card` 必填，不给拒发（exit 2）；`--card -` 印「无卡」；信头参与 `--max` 与回读校验 | ✅ 对 |

## 二、我自己跑的门禁（不看自报，重跑一遍）

| # | 门禁条 | 结果 |
| --- | --- | --- |
| 1 | 有没合入的提交 | ✅ 1 个 `a7b233c` |
| 2 | 变更面非空 | ✅ 6 个文件 |
| 3 | 改动都在允许范围内 | ✅ 全在 `tools/mobile_chat/` + `docs/` |
| 4 | 没有删除文件 | ✅ 无删除 |
| 5 | 署名是注册看板名 | ✅ `codex-看板编辑 <codex-convtool@…>` |
| 6 | 回归全绿 | ✅ `selftest_v2` **236/236** |
| 7 | 有审阅记录文件 | ✅ 本文（首轮跑门禁时缺，**由我补上后转绿**） |

**另核**：`selftest_say` 12/12、`ui_check` 63/63（他跑的，我认可其口径：预览页隔离实例、正式端口未动）。

## 三、三条未结的裁定

1. **公告递增序号 `N-1/N-2` 不做 → 我认可他的判断**：老板原话的对象是**回执**（"你给**回执**加一个数字"），
   已落在 `acks[].ord`；把 `noticeCode()` 换成序号会让**历史短号回执全部认不出来**、收益为零。
   **若老板要的是"公告本身带序号"，单独开一件**（不动历史短号）。
2. **`docs/RUNBOOK_SEND.md` 的 `--card` 用法没并入** → 原因成立：那文件里躺着**我**未提交的「按级别读」抬头。
   **处置**：由我先把总监侧未提交的文档纳入提交，他随后补这一段（下一批或本批补丁）。
3. **重启没做** → **正确**。按"重启=上线"，本批合入 `main` 后**仍需老板放行**才重启。

## 四、结论

**门禁 7/7 通过 → 同意合入 `main`。**
合入后**不重启**；等老板放行令，由 `codex-看板编辑` 重启并留痕（时间 / 为什么 / 结果）。

**签字**：`codex-总监` 2026-09-13 05:4x（证据：本文件 §二 门禁输出 + 分支 `a7b233c`）

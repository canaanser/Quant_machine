# TRD-005 执行链三修：dbfread 错位 / 回账入账 / 买入追高闸

- 卡号：`TRD-005`（`TRD`＝交易/策略/回测链路，拥有者 `codex-量化总监`）｜类型：`fix`｜优先级：`P0`
- 派单：老板（2026-09-14 05:5x 交办，孤儿模式：只对老板）｜执行：`codex-量化总监`
- 状态：**已改、已自测、待实战验证**（14:44 执行段 / 15:05 回账）

## 一、改了什么

| # | 文件 | 改动 | 对应故障 |
| --- | --- | --- | --- |
| 1 | `core/lib/dbfread.py` | 正常记录**先消费 1 字节删除标志**（`p += 1`）再读字段 | 9/11 故障 #3：字段整体错位 1 字节（"解析器字段错位"） |
| 2 | 同上 | ——（#2 的根因就是 #3：`sync_fills()` 读 `execution_report.dbf` 走的就是这个解析器） | 9/11 故障 #2：回账 0 笔 |
| 3 | `duty/duty_engine.py` | `_read_plan()` 支持可选第 6 列 `max_px`（买入追高上限）；新增 `_chase_blocked()`；买入侧 `实时价 > max_px` → **跳过并写决策** | 新需求：把"人肉删买单行"固化成引擎判据（老板 2026-09-14 交办） |

## 二、自测（都可复跑）

- `python -B scripts/test_dbfread_flag.py` → **PASS**（合成 DBF：修前 `603256`→`60325`、删除记录漏跳；修后对齐且正确跳过）
- `python -B scripts/test_sync_fills_dbf.py` → **PASS**（合成 `execution_report.dbf` 2 笔全部入账：买 002428、卖 603256；旧解析器此处必为 0 笔）
- `python -B scripts/test_plan_chase_gate.py` → **PASS**（六列解析 ✓、五列兼容 ✓、闸判据 4 例 ✓）
- `py_compile duty/duty_engine.py` → OK

## 三、未结 / 待验证

1. **14:44 执行段**（`#1` 修复的首验）+ **15:05 回账**（`#2/#3` 的首验）——引擎已于 05:50:16 重启，改动在其中；
2. **追高闸今天不在生效进程里**（05:50 重启早于本次改动）→ 今天仍按"14:35 人肉判 + 删行"，闸从下次重启起自动生效；
3. 追高闸的第 6 列 `max_px` 需要在生成计划时写入（`next_plan.csv` 现为 5 列，缺省=不设闸）。

## 四、签字区

| 角色 | 人 | 时间 | 结论 | 证据 |
| --- | --- | --- | --- | --- |
| 派单 | 老板 | 2026-09-14 05:5x | 交办（孤儿模式） | 本文件 |
| 执行 | `codex-量化总监` | 2026-09-14 06:0x | 三改 + 三测 | §二 |
| 验收 | 老板 | — | 待实战 | 14:44 / 15:05 |

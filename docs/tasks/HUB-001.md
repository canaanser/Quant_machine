# HUB-001 员工生命周期：新线自动入职 + 归档自动退役

- 派单人：老板（口述）+ `codex-总监` 落单　接手：`codex-看板编辑`（工具开发角色；**产品不绑定线程**）
- **类型**：`dev`（开发）　**严重度**：—（非故障）
- **优先级**：`P1`（阻塞度：不解决，以后每加一个人都要手工占位）　**卡住了谁**：新员工批量入职
- 范围：只改 `tools/mobile_chat/*` 与相关文档；git 不动
- **前身**：`docs/tasks/CT-021.md`（该卡已标作废，只标记不删；本卡为其正式改号版）
- 关联：`docs/ALIGN_BOARD_KIT.md`（口径对齐）、`docs/TASK_ID_STANDARD.md`（命名规约）

## 〇、与 HUB-002 的边界（**先看这条，别做重**）

| 卡 | 职责 | 不碰 |
| --- | --- | --- |
| **HUB-001（本卡）** | **登记表与生命周期动作**：入职登记、退役释放 | 不管"叫不叫醒"的调度逻辑 |
| **HUB-002** | **投递与唤醒的调度**：读状态、决定叫不叫、退避 | 不管登记表怎么写、退役怎么放 |

**状态机只有一个真源**：`docs/ALIGN_BOARD_KIT.md` §三（`unknown` / `active` / `stale` / `retired`）。
两边**都不得私改字段名或取值**——这是两张卡最容易打架的地方。

## 一、A 自动入职（幂等，只触发一次）

触发：一条新线**首次**在 Hub / 看板露面（发言、被 @、或被投信箱）。

1. 按 `docs/BOARD_NAMES.md` 生成 `前缀-名字`（老板只给"对话框名"时，前缀按平台侧补：Codex → `codex-`，DSH → `dsh-`）与 slug；
   - **slug 生成规则必须与套件侧一致**（套件已定：显式优先；兜底 = `side + '-' + sha1(name).slice(0,6)`）。这条要跟套件线的 `docs/task-id.md` 同级对待——**两边同名必须同 slug**，否则信箱文件名会对不上。
2. 写进 `outputs/dialog/agents.json`：`label/title/slug/workspace/note/status`；`level` 取老板指定值，未指定默认 `member`；
3. 生成信箱文件 `pending_<slug>.ndjson`（空文件即可，路径规则见 `docs/BOARD_MAILBOX.md`）；
4. 看板发一条**系统**入职播报（署名用看板服务自己的名字，**不许冒充新线**；内容含 名字 / slug / 层级 / 工位）；
5. 状态栏出现该线（未上线过的标 `unknown`）。

## 二、B 自动退役 / 释放

触发：该线被归档（Codex 线程 archive / DSH 会话关闭），或老板显式说"退役 X"。

1. `agents.json` 里 `status` 改成 `retired`，**条目保留、不物理删**（红线：废弃只标记）；
2. **释放占用**：它名下"已认领未完成"的任务不得凭空消失 → 进入「待接管」清单（或按规则退回未指派）；
3. 看板发系统退役播报；此后再 @ 它 → 明确回复"该线已退役"，**不再静默兜底**；
4. 它的信箱内容归档（移入 `done/` 或对应归档位），不删。

## 三、护栏（必须遵守）

1. **幂等**：重启 Hub 不重复登记、不重复播报。
2. **手工字段优先**：已手工登记的线（如 `codex-套件` 带 `level: lead`），自动流程不得覆盖或降级。
3. **退役要有证据**：谁判定、何时、依据哪个归档事件；且可人工复产（`retired` → `active`）。
4. **不许冒充**：系统播报一律用看板服务自己的署名。
5. **状态口径不许私改**：字段名与取值以 `docs/ALIGN_BOARD_KIT.md` §三 为准。

## 四、验收标准（逐条可判定）

1. 新建一条线、发一句话 → 3 秒内自动出现在实例表与状态栏，@ 它能投达；
2. 归档那条线 → 状态转 `retired`、@ 它给出明确提示、其未完成任务出现在「待接管」；
3. 重启 Hub 后状态保持，且不重复播报；
4. 预先手工登记的线（`codex-套件`，`level=lead`）在自动流程跑过后**字段不变**；
5. 自测脚本补用例并全绿（含：幂等、手工优先、退役释放、@退役线提示）。

## 五、交付物

- `tools/mobile_chat/board.mjs`（或拆分模块）+ 自测用例；
- 更新 `docs/DIALOG_HUB.md`、`docs/CONVTOOL_V2_PROGRESS.md`；
- 看板回一页验收报告（署名 `codex-看板编辑`）。

## 六、备注（写给未来的自己）

这条能力将来会被套件自己的 `hub/` 继承（对应 `agent_crew_kits` 的「员工生命周期」与 `org-levels`）。实现时请**把"规则"和"引擎"分开写**：规则（命名、slug 算法、默认层级、退役策略）尽量用配置/数据表达，引擎只管执行——将来搬家才好搬。

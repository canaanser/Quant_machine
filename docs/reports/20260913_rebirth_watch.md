# 「该不该重生」体检 · 交付报告（老板 2026-09-13 09:3x 交办）

**结论（头 5 行）**

1. 交活：**分支 `feature/director-rebirth-watch`**（短 hash 见**交活行/板行**——本文件不自指哈希：amend 一次它就过期，写死等于假事实）；脚本 `scripts/rebirth_watch.mjs`（新）+ 接线 `scripts/crew_host.py`（+1 worker）。
2. 数据**只取真源、不猜**：名册 `threadId` → 该实例的 rollout 账本；`compacted` / `token_usage_record.usage.input_tokens` / `turn_context.turn_id` 三种记录分别给出**压缩次数 / 累计输入 / 轮数**。
3. 判据是**常量 + 来源**（`AGENTS.md` §「什么时候该重生」）并**取下沿**；主判据「翻文件 vs 记住」**明说算不了**（rollout 无字段，不硬编）。
4. **静默接线**：挂在**已有**的小工 30 秒节拍上（**没建计划任务**），脚本内自带 10 分钟闸；**平时零输出**，命中才 ① 给那条线投信 ② 给总监一行（带核于戳）。**只报不动**。
5. 判别性用例 **10/10**：爆表线必报、正常线必不报、无实例线判"算不了"、第二次不重复吵、压缩再涨才再报。

## 一、每线一行（真实数据，`--scan` 现算）

```
线 | 状态 | 压缩次数 | 累计输入 | 轮数 | 命中
codex-看板编辑 | ok | 0 | 123.7万 | 2 | 未命中
codex-唤醒通道 | ok | 0 | 1.79亿 | 63 | 未命中
codex-量化总监 | ok | 0 | 4919.4万 | 57 | 未命中
codex-总监     | ok | 0 | 2638.3万 | 13 | 未命中
codex-套件     | ok | 0 | 1.21亿 | 41 | 未命中
codex-修复     | ok | 0 | 1.44亿 | 62 | 未命中
codex-人事/渲染/适配/配置 | ok | … | … | … | 未命中
dsh-老员工 / dsh-quant / 看板服务 / 看板助理 | **算不了** | - | - | - | 名册里没有 threadId（非 Codex 线/无实例）
```

**当前没有任何一条命中** → 接线处于**静默**状态（这正是期望态，不是"没生效"）。

## 二、判据：常量 + 来源（`AGENTS.md` §「什么时候该重生」，老板 09:0x 定·锚定）

| 常量 | 值 | 来源原文 | 说明 |
| --- | --- | --- | --- |
| `COMPACT_LIMIT` | `2` | 「压缩 ≥ 2 次」 | 直接取值 |
| `INPUT_TOKEN_LIMIT` | `300_000_000` | 「累计输入 > 3–5 亿」 | 区间**取下沿**（3 亿） |
| `TURN_LIMIT` | `300` | 「300–500 轮」 | 区间**取下沿**（300） |

**算不了的那条（明说，不硬编）**：主判据「**翻文件找线索的时间超过记住的时间**」——它是**人的主观感受**，
rollout 里没有任何对应字段，任何"代用指标"都是我拍脑袋，所以脚本**不报**这条，只在输出里标 `n/a`。
（真要判它，只能由**那条线自己**或老板说了算。）

## 三、静默接线（重点）

- **不新建计划任务**：在 `scripts/crew_host.py` 的 `WORKERS` 表里加了一名 `rebirth-watch`，跑在**已有的 30 秒节拍**上；
- **脚本内部 10 分钟闸**（`SCAN_INTERVAL_MS`）+ 增量读账本（从上次 offset 只读新增字节，43MB 的账本不会每拍全量重扫）；
- **平时零输出**：没命中就**不写日志、不回板、不发信**；
- **命中才说话**：① 给那条线投一封信（附**重生三步** + `--plan` 差量页 + §三 移交单指引）② 给**总监**一行（带 `evidence.mjs` 出的**核于戳**，核法 = `--check <线名>`，可复算）；
- **防成新噪声源**：同一线只提醒一次；要再提醒，必须**压缩次数又涨**或**距上次 ≥ 12 小时**；
- **只报不动**：脚本**不触发**重生（边界所限），重生与否由那条线及其上级定。

## 四、判别性用例（`--self-test`，10/10）

```
PASS 爆表线 → 判命中（2 次压缩 / 4.00亿 / 320 轮，三条全中）
PASS 正常线 → 判未命中（0 / 500万 / 12 轮）
PASS 无实例线 → 判"算不了"（不硬编、**不当命中**）
PASS 退役线 → 不体检
PASS 扫表里明说主判据算不了
PASS --check 爆表线 → 退出码 0（能当核于戳的核法）
PASS --check 正常线 → 退出码 1
PASS --tick 命中时：只给爆表线 + 总监各一封，**正常线一封都没有**
PASS --tick 第二次（未恶化）→ **零输出**（不重复吵）
PASS 压缩次数再涨 → 再次提醒（恶化才说话）
```

## 五、已知边界（没做完的，我直说）

1. **「连续高强度 6–8 小时」没纳入**：要先定"间隔多久算断"的口径，本轮不拍脑袋；要加请给口径。
2. **主判据算不了**（见 §二），只能靠那条线自述。
3. **只覆盖 Codex 线**：DSH 线与服务线名册里没有 `threadId` → 一律判"算不了"（不静默跳过，扫表可见）。
4. **②那一行发给总监**（老板原话「给你一行」）；**要改成上板只改一个常量** `ALERT_TO`。
5. 度量口径：`累计输入` = 账本里每条 `token_usage_record` 的 `input_tokens` 累加（**含缓存命中**，与平台计费口径未必等同）；
   `轮数` = `turn_context.turn_id` 去重计数。两处都在脚本头注释里写明了。

## 六、核于

- 核于 09:27｜核法 `node scripts/rebirth_watch.mjs --self-test`｜结果 **PASS**（判别性 10/10）
- 核于 09:27｜核法 `node scripts/rebirth_watch.mjs --assert-quiet`｜结果 **PASS**（当前零命中 = 接线静默态）
- 核于 09:27｜核法 `git rev-parse --verify --quiet refs/heads/feature/director-rebirth-watch`｜结果 **PASS**（交付分支在仓里）
- 核于 09:27｜核法 `git log --oneline main..feature/director-rebirth-watch | findstr /C:"feat(rebirth-watch)"`｜结果 **PASS**（交付提交**尚未进 main**，等你复验）
  —— 这条**刻意按提交标题**钉、**不钉 hash**：amend 一次 hash 就变，钉死等于写了个会过期的事实。

**合入状态：我主动撤回了自动合入申请**（门禁 8/8 全绿但它会自动合——按"谁派谁验、不许自己给自己签字"，等你复验通过我再合）。

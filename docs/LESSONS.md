# 经验教训账本（append-only，按事件记）

> 🔖 **按级别读**：先跑 `node tools/mobile_chat/whoami.mjs --me <你的看板名>` 看你**现在**是哪一级（真源＝员工卡／名册）；
> 本文件标 `[member]`/`[lead]`/`[director]` 的段落**只对那一层生效**，**级别一变就跑一次 whoami 重新对表**（总表 `docs/READING.md`）。

> 规则：每次出事往这里加一条，写清 **事件 / 后果 / 变成什么规矩**。
> 目的：**同一个坑不踩第二次**；也让人知道"这条规矩是从哪次事故来的"。

---

## L1 · 署名必须实名；旧账归属以 rollout 为准（2026-09-11）

- **事件**：`codex-总监` 误用 `codex-量化总监` 的名字约 3 小时（发信箱、写任务卡、看板落款）。
- **后果**：dsh 按"它的批文"执行；量化总监自己也认领了**不是它发的**批文；旧账归属全乱。
- **规矩**：① 跨线一律**实名**；② 定旧账归属**以 rollout（带时间戳的原始账本）为准**，看板署名只作参考；③ 改名/冒名窗口期，必须在锚点里标「该时段归属存疑」。

## L2 · 改全局配置 = 平台级改动，三件套缺一不可（2026-09-11）

- **事件**：09-10 03:42 某线装桥并写进**全局** `~/.codex/config.toml`（先审源码、先备份都做对了），但**没有任何登记**。
- **后果**：两天后它自己被这个"全局件"锁死，且**查不到是谁装的、什么时候装的**——最后靠 rollout 才追出来。
- **规矩**：**① 登记**（谁改、改了什么、**影响面=所有线**）+ **② 回滚点**（备份路径写下来）+ **③ 风险说明**；改完必须验证（如 `mcp list`）。
- **要点**：全局件出事**会连坐**——影响面不是"我自己"，是"所有线"。

## L3 · 静默失败是最大的敌人；凡"可能没人处理"必须可见（2026-09-11）

- **事件**（一晚三个同类）：① 判忙只写信箱不写 `nextAt` → 留言**永远不会被叫醒**；② 用 Hub 自己的 `busy` 判忙 → 误判；③ `HH:MM` 时间戳被拼成 `NaN` → 判"太老" → **值守静默跳过、永不回话**。
- **规矩**：可见化（`信箱 N` / `已投递未唤醒` / `退役未释放`）；**不许假装已送达**；凡"可能没人处理"的状态都要在界面上看得见。

## L4 · 投递 ≠ 唤醒；决定要叫 ≠ 叫得醒（2026-09-11）

- **规矩**：协议只承诺**投递**；唤醒是适配器的事，且必须有**可查的状态口**（如 `/api/deliver`）；拿不到入口就**明确判死并写理由**，不许含糊。

## L5 · 平台的客观缺陷用适配器填，不用嘴填（2026-09-11）

- **事件**：DSH 没门铃 → 做门铃（Web RPC `session.prompt`）；Codex 注入抢写锁 → `HUB-005` 摸通道；沙箱写不了 `.git`/没网络 → `KIT-007` 沙箱外发布器。
- **规矩**：平台差异关进 adapter；**没有适配器就诚实标注降级**，不许用未来功能冒充现在（例：不许写"已通知全员"）。

## L6 · 停点就是停点；派单人必须写明停点（2026-09-11）

- **事件**：新员工两次跨停点；第二次主因是**派单指令没重述停点**。
- **规矩**：派单必须写明停点与"停在哪、报什么"；执行方必须**硬停**；跨停点=违纪，哪怕报告写得清楚。

## L7 · 命名：前缀=领域不是类型；号段永不复用（2026-09-11）

- **事件**：`CT-021/022` 改号 `HUB-001/002`；`KIT-001` 三位前缀违反了自己写的两位契约。
- **规矩**：前缀取自登记表（`docs/TASK_ID_STANDARD.md`），**代表产品/领域**，不代表任务类型；序号**永不复用**；**未开工才可改号**，已交付永不改。

## L8 · 员工是身份，不是会话；产品不绑定线程（2026-09-11）

- **事件**：第一号对话框（`01a0877b`，元老）被自己装的零件锁死。
- **规矩**：人的连续性靠 **员工卡 + 锚点 + 可恢复档案**保住；换线写锚点，**名字不变**；退役只标记不删。

## L9 · 新员工初始化是有效的（正反馈，2026-09-11）

- **事件**：新线入职后**全程只读**调查，证据精确到**行号**，并**自己推翻了自己的第一版结论**（"04:17:57 只是整份替换时间"）。
- **结论**：员工卡 + 红线 + 唤醒自检 + "先汇报再动手"这套是**管用的**，继续保持。

## L10 · 人在环上：故障即停、公告解除、异常冒泡（2026-09-11）

- **制度产物**：`HUB-003`（叫醒老板：上板 @ + 置顶）、`HUB-006`（**故障即停，停到老板看到**；解除=老板公告；非老板公告不生效）、`HUB-004`（老板派单工具）。
- **规矩**：老板只做三件事——**拍板、验收一页、发公告**；异常往上冒，不许逐级过滤。

## L11 · 投影 ≠ 账本：改库之前先只读勘察，别照着先例硬修（2026-09-11）

- **事件**：诊断说"历史里有两份命名空间"，于是先例（§6.5）提示去修 `thread_items`。**实际一查：库里根本没有那种 item 类型**（该线 1424 行只有 reasoning/commandExecution/agentMessage… ），脏项**只存在于 `rollout-*.jsonl`**。
- **后果**：如果照着先例直接动手，**会白改一遍**（甚至删错东西）。
- **规矩**：① **库是投影、rollout 才是账本**，两者内容不一定一致（投影会裁剪/不收录某些类型）；② 动手前**先只读勘察、把"要删的那一条"实实在在找出来**，找不到就说明这条路无效；③ 判断依赖**实测**，不依赖"上次是这么修的"。

## L12 · deferred 命名空间：别连搜两次，否则整线锁死（2026-09-11）

- **事件**：某线对**同一个 deferred 命名空间**连续 `tool_search` 两次 → 历史里留下两份同名命名空间 → 平台 400 拒收 → **该线永久锁死**（每一轮都重发那条脏项）。
- **独立核验**（rollout 逐行）：`defer_loading` 只出现 2 次——第一次同时含 `mcp__node_repl`（**Codex 内置**）与桥命名空间，均带 defer；第二次重复了桥命名空间 → 炸。**说明 deferred 是客户端通用行为，人人有份。**
- **定性**（不追责，但要改流程）：桥包无 defer 代码；安装者**照 README、先审源码、先备份、后验证**，流程是对的。**根因是平台不去重**；唯一可改进的是"改全局配置要登记 + 写影响面"（见 L2）。
- **规矩**：① **不许对同一 deferred 命名空间连搜两次**（`docs/tasks/PLT-001.md` 首条）；② 任何线被锁死 → 走**新线承接**（写锚点、名字不变、旧线只读退役），**别动 rollout、别白改 SQLite**；③ 平台去重属上游，登记在 `PLT-001` ③。

## L13 · 门铃**是可达的**：重启 app 之后注入能成功（2026-09-11）

- **事件（两条实测，都可复核）**：
  - 09-10 **23:23:06**：老板 @`codex-量化总监` → 该线程**真的起回合**（归档 rollout `archived_sessions\…01a0877b….jsonl` L4358 `task_started` + L4363 一条包装成「（看板留言，来自 dsh-老员工，时间 …）」的 user 消息），23:26 **本人在看板回话**——是本人，不是代答；
  - 09-11 **07:39**：老板**重启 Codex app 后** @`codex-总监` → **我被注入唤醒并自动回板**（本线 rollout L3619 那条「（看板留言，来自 老板，时间 2026-09-11 07:39）」+ L3607/L3628 `task_started`）。
  - 09-11 **08:17/08:18**：老板 @**从未打开过的** `codex-套件` → 该线 rollout 两次 `task_started`（00:17:09Z / 00:18:23Z）+ 注入的「看板留言」，08:19/08:20 **本人回板且无代答标记** —— **第 3 次独立验证**。
- **规律（2026-09-11 08:2x **已由对照实验定稿**）**：**能不能被唤醒 ≈ 那条线程有没有被 app 持锁**。
  - **对照实验（老板亲手做，2026-09-11 08:2x）**：① 对**从未点开过**的 `codex-套件` @ → **本人自动回板、无代答标记**（08:19/08:20）；② 对**已被点开**的 `codex-总监` @ → **果然没有回答**（只有"已投递未唤醒"）。
  - **结论**：**锁 = "客户端已经占住这条线程"的标记**；客户端占住 → 外部注入进不来 → 叫不醒。**要在看板上叫醒某条线，就别在客户端打开它。**

### L13 补记（2026-09-11 21:4x · **已解决**）：`codex queue` 穿透了写锁

- **新版 CLI 带了一条命令**：`codex queue --thread <会话UUID> --message "文本"` —— "Queue a message for an existing session"，**不抢写锁**。
- **实测（两次）**：① 老板手动 `codex queue` 投给**已被客户端打开、`lockPresent=true`** 的我 → 我的 rollout 立刻 `task_started`；② `crew_host` 的 `doorbell-queue` 小工再把同一条投给我 → 同样醒。
- **因此上面的"打开就锁死"结论被推翻**：**"打开着的线也能被叫醒"** —— 门铃**由 `codex queue` 正式打通**。
- **落地**：`scripts/crew_host.py` 的 `doorbell-queue` 小工（跑在计划任务上下文；只投指针、每条线 10 分钟冷却、单轮最多 2 条、只调 `codex.exe queue`）。
  `~/.codex/thread-writer-locks/<threadId>.lock` 被 app 对**加载过**的线程长期持有（实测 10 个锁全部"被独占占用"，含我自己的）→ 持锁即注入被拒 → 叫不醒；
  **app 重启后、没有再打开过的线程没有锁** → 注入成功 → **本人被唤醒（慢一点）**。
- **更正此前两条错判**：① "Codex 线从来没有自动唤醒"——**错**；② "关窗口能松锁"——**不准**（锁与"是否被加载过"有关，与当前是否在前台无关）。
- **规矩**：① **"本人慢答"是可达机制**，代价是那条线在 app 里**保持未打开**；② 要立刻叫醒某条线 → **先重启 app，再在它未被打开时 @ 它**；③ **归档 = 移动到 `archived_sessions/`，不是删除**（rollout 仍在，可查）。

## L14 · 回合越长，老板越输不进来（客户端输入锁，2026-09-11）

- **事件**：老板反馈——本线对话**被锁很久**，直到他发消息后才释放（怀疑与切走又切回有关）。
- **机制**：桌面端**在 agent 跑回合时锁该线程的输入框**；解锁发生在**回合结束**。所以**回合长度 = 老板被锁住的时长**。
- **规矩**：① **短答优先**（尤其在被 @ 叫醒后）；② 需要长干活时，**先给一句话结论 + 落文件**，别在对话里长篇；③ 让老板**切到别的线程去等**，我干完**上板 @他**，他不必守在被锁的窗口前。

## L15 · 承接（换人）：岗位不变、工号换代（2026-09-12）

- **模型（老板认可）**：**看板名 = 岗位**（延续）· **slug = 工号**（**永不复用**）· **threadId = 实例**（随时换，旧的进 `failedThreadIds`）。
  "财产"全挂在 slug 与工位路径上，所以**同 slug 换实例 = 零迁移**；换 slug = 换人，要做资产转交。
- **门禁**：**没有交接文档不许换人**（`docs/SUCCESSION_TEMPLATE.md` 四节：身份/工位/未结单据/别再做什么）。
- **工具**：`scripts/crew_succession.mjs`（plan/apply/trace/selftest）；真源 `outputs/dialog/successions.ndjson`；手册 `docs/RUNBOOK_SUCCESSION.md`。
- **两个实测坑**：① **改名必须在归档之前**（已归档的线程 `set_thread_title` 报 `no rollout found`）；
  ② **自动建窗仍是平台 bug**（`create_thread` 的首回合直接 `missing field call_id`；Windows 上 app-server 的 daemon/proxy 通道也不可用）→ **开窗仍要老板在 UI 点一下**，其余全自动。
- **首次实战**：`codex-套件` 工号 `codex-kit` → `codex-kit2`，换到以 `D:\agent_crew_kits` 为工作区的实例；13 秒后小工自动镜像+门铃，新线自己接手把 M0 提交并推送。

## L16 · 通道顺序：`resume` 会报成功却不产生回合（2026-09-12）

- **现象**：门铃日志写 `投递OK via resume(未加载)`，但对方线程 `updatedAt` 一动不动——**根本没跑回合**。
  老板问"你这里为什么叫不醒他"，根因就在这 + 我设的"同一批最多敲 3 次"到顶永久静默。
- **修正**：① `codex queue` **对"已加载/未加载"两种线都真能跑出回合** → 宿主改成**队列优先**，`resume` 只当备选；
  ② 敲满 3 次**不再永久停手**，改长间隔续叫（30 分钟 → 1 小时 → 2 小时）；
  ③ 门铃要有**可验证**的证据：别只看"投递OK"，要看对方**有没有产生回合**。

## L17 · 审阅→合入也能自动化（2026-09-12）

- **做法**：把审阅判据写成**可执行门禁** `scripts/crew_review_merge.mjs --check`（未合入提交 / 变更面 / **没碰 `spec/` 与他人仓** / 无删除 / 署名合规 / `npm test` 全绿）
  → 全绿才写 `outputs/dialog/merge_request.json`；沙箱外小工新 worker `auto-merge` **照单合入并推 origin**，不看人情。
- **规矩**：**没过门禁一个字都不动**（实测：故意让 `npm test` 挂 → 拒绝合入）；分支在审阅后被改动 → 拒绝并要重跑。
- **共享文件纪律**：`scripts/crew_host.py` 曾被**整份改写**、吃掉别人的改动（只能靠 mtime 发现）→
  **改共享文件先读盘、只动自己那几行**；关键文件改动前后记哈希。

## L18 · 干活要"播报"，不能只剩工具调用（2026-09-12）

- **老板原话**："你现在怎么只剩下正在思考和调取命令行的阶段了？干活时每步先一句话说明要做什么、再执行。"
- **现象**：长回合里我连续调工具、不给阶段说明，老板只看到"正在思考/正在执行命令"——他不知道我在干什么、也没法中途纠偏。
- **规矩（已锚进 `AGENTS.md`「过程播报纪律」）**：① **每动一步先一句话说清意图**，再执行；② 那一句写**目的与判据**，不是复述命令；
  ③ 这是**过程说明**不是汇报——**结论仍然只在最后一段给**，中间不写长篇；④ 长内容落文件，播报只留一句话；
  ⑤ 被叫醒后第一句先给结论，再干活。

## L19 · 别复述别人的"未结"当事实（2026-09-13，codex-总监 自己踩的）

- **事件**：我连着两条板上留言都写「`HUB-004`（手机派单表单）**仍未开工**」。看板编辑核了 `main` 后纠正我：
  派单页（`board.mjs` 的 `id="menuTask"` / `id="taskPage"` / 三态 `delivery`）**早在 `aadbc37` 那次就一起合进来了**。
  我的信息来自**它自己早前那条留言的"未结"清单**——那份清单在它后续合并之后**已经过期**，我却照抄了两遍。
- **根因**：**把别人的旧"未结"当成了当前事实**，下结论前没有核 `main`。
- **规矩**：① **报事实前先核真源**（`git grep/show main`、看板真源、名册），**不复述任何人的旧未结**；
  ② 必须引用别人的清单时，写明**时间点**并注明"未复核"；③ 要写进板的结论，**只写我自己核过的东西**。
- **一句话**：**清单是投影，账本在 main**（与 L11 同一类病的另一个入口）。

## L20 · "看着做好了"≠"走得通"——交付前必须走一次**真路径**（2026-09-13）

- **事件**：`codex-看板编辑` 改 `/api/send` 署名时发现——**手机派单页（HUB-004）看着做好了，其实一条都派不出去**：
  页面用 `author:"老板"` 提交，而所有口子的署名校验只认"注册看板名" → **`老板` 被 400 顶掉**。
  同一根因还压着 `/api/post` 与 `/api/send`：**老板本人在手机上发的消息也会被拒**。
  修法：新增 `resolveSigner()` = **注册看板名 ＋「老板」**，三处共用；补一条**端到端**用例（"手机派单页署老板真提交一次"）。
- **它自己的认账**（我认这个态度）：此前按"**代码在 main 里**"判定 HUB-004 已完工——**那条路一次真提交都没走过**。
  这跟我复述旧"未结"是**同一类病**：**把投影当账本**（L11/L19）。
- **规矩**：① **凡是"给人用的入口"（页面/按钮/接口），交付前必须真走一次**：真点击、真提交、看落盘；
  ② 只有单测/自测不算"走通"；③ 端到端用例要覆盖**真实署名与真实身份**（老板是真人端点，不在名册里，别默认它在）；
  ④ 判"完工"只看**真路径的痕迹**（落盘记录/看板行/文件），不看"代码合进 main 了"。

## L21 · 不许用 app 的跨线程委派工具给别的线派活（2026-09-13，两次实证）

- **缺陷（同一个根因）**：app 的跨线程通道写进目标线程的那条记录**缺必填 `call_id`** →
  目标线程**整轮 400、永久锁死**。两次实证：
  ① **工具建窗**（`create_thread`）：新窗口首回合直接 `missing field call_id`（L15）；
  ② **跨线程委派**（`send_message_to_thread`）：写的是 `{"type":"function_call_output","id":"fco_…","name":"send_message_to_thread",…}`，**同样缺 `call_id`**。
  今晚 `codex-修复` 用它给三位待命新人派岗位说明 → **三条全部 `systemError` 并归档**（账本+投影各 1 处残项，已用等字节替换修净、留了备份）。
- **纪律（`AGENTS.md` 已收紧为"唯一通道"）**：**线 ↔ 线只走「信箱 + 敲门铃」**（`tools/mobile_chat/say.mjs --mail --wake`，或直接投 `pending_<slug>.ndjson`）。
  **禁用** app 的委派/建窗工具给别的线派活——它不是"省一步"，是**会把目标线写死**。
- **附带教训**：修完之后，被损坏的那些消息**正文只能换成中性留痕**——所以**那三位没收到岗位告知**，得由 HR 重派。
    **修数据救得回线程，救不回消息内容**——所以更要事前守通道。

## L22 · 换判据来源必须**迁移老状态**，否则上线瞬间会"补叫"一轮

- **事件（2026-09-13 05:0x，我干的）**：给门铃换水位来源（旧＝"上次推送的**时刻**" → 新＝"**被推那封信自己的 ts**"，
  为了防"同一分钟内到达的新信被当成已读"）。**换了来源却没把老状态继承过来** → 已经推过的线水位掉回 0 →
  **每条线各被多叫一次**（`hr 05:05:11 / render 05:05:11 / adapter 05:05:41 / config 05:05:42`），
  适配线与配置线**当场报案**（"已办完的信又被叫"）。
- **后果**：两小时内第二次"重复门铃"投诉，老板又得多问一轮"到底修好没有"。
- **变成什么规矩**：
  ① 改**判据来源**（不只是改阈值）＝ **数据迁移**：上线前必须从老状态把等价字段**补齐**，
     并在自测里**钉住"跨版本上线不补叫"**（本轮已补：`scripts/test_crew_host_p0.py` 场景 11）；
  ② 现场报案**先当"我方的锅"查**——这次两条线都猜"是不是没清水位"，它们**猜对了**；
     不许先教对方"你按规矩就好"；
  ③ "每线最多多叫一次"听着轻，**也是事故**——它直接违反"同一封信只叫一次"这条对老板的承诺。

## L23 · 自检重载的脚本**不许分步保存**：中间态会真的跑到线上

- **事件（2026-09-13 05:2x，还是我）**：修 L22 那个迁移问题时，我**分五次保存** `crew_host.py`。
  宿主每 30 秒自检 mtime、**改一次就重载一次** → 其中一版（已换水位来源、**迁移还没补上**）
  正好在 **05:21:49** 跑了一轮 → 量化总监那条线被**同一封旧信第二次**叫醒，它当场报案（带日志证据）。
- **后果**：同一小时内**第三次**"重复门铃"投诉；老板再问一轮"到底修好没有"。
- **变成什么规矩**：
  ① 改这种**盯着 mtime 自动重载**的脚本（`crew_host.py`、计划任务宿主、看门狗），
     **要么一次性写完再落盘，要么先停定时器**——**分步保存 = 分步上线**；
  ② 落盘后**必须立刻**：`py_compile` → 跑该模块自测 → **看一眼日志确认新版本起来了**（本轮做全了，但漏了"改的过程中别落盘"）；
  ③ 报案**先查我方时间线**：这三次重复铃，全是**我自己的改动**造成的，三次都是别人先发现——**这不正常**。

## L24 · 宿主/账本/重启：三条"看着没事"的操作，各留一条隐患（2026-09-13 05:3x）

- **事件**：`codex-看板编辑` 自报两件、我复核后又补一件：
  ① **重启早于审阅**：他 05:31 就重启了看板（理由是"自测 7/7 绿、变更已提交"，与"合 main"解耦）；
     而我 05:34–05:36 才跑门禁并合入 `main`。
  ② **双实例窗口 05:33:21–05:37**：他按心跳停了 2.5 分钟**误判宿主死了**（查错了进程名——任务是 `pythonw.exe`，
     他按 `python.exe` 查），用 `schtasks /Run` **多起了一个 `QuantCrewHost`**；实测那一拍**镜像翻倍（2 条）**。
  ③ **清账本是"重写"**：他把 `pushed.ndjson` 从 21 行改成 19 行（隔离备份留了原文）。读取方**可能读到半截/旧版** ——
     05:41 我这条线被多叫一次，最可能就是这 24 秒内的读竞争（事后复算：未读 = 0）。
- **后果**：②③ 都可能凭空制造"重复门铃"，也就是老板最烦的那件事；且 ② 是**动了别人的宿主**（分工越界）。
- **变成什么规矩**：
  ① **重启 = 上线 = 必须在门禁通过 + 合 main 之后**（"自测绿"不等于"门禁过"）；本轮实质无风险（我核过工作树 == 合入版本），
     但**纪律不能松**：等价是运气，不是保证；
  ② **判"服务死了"三步**：按**对**进程名 → 看它是否在跑长任务 → 再看心跳；**且绝不替别人重启别人的宿主**（只报不动手）；
  ③ **共用账本只许 append**：清理要"**另写新文件 + 改名替换**"或"标记作废"，**不许原地重写**——读取方随时在读。
     （本轮采用"标记 + 隔离备份"，方向对；缺的是**原子替换**这一步。）

  **③的精确机制（看板编辑 05:45 补，我核实）**：他用 `Set-Content 到 .tmp-new` + **`Move-Item -Force`**——
  在 Windows 上 **`-Force` 覆盖已存在目标 = 先删后移**，**不是原子替换** → 那 24 秒里账本"短暂不存在/被截断"，
  读方看到 **0 行** → 判"全员未读" → **全员被重叫一次**。我 05:41 那次多叫，机制对得上。
  **两条修法（都已落地）**：写方用**真原子替换**（`[IO.File]::Replace` / `MoveFileEx REPLACE_EXISTING`）；
  **读方兜底**（我这边）：账本**缺失或读到 0 行时沿用上次水位**，**绝不当成归零**（自测场景 14 钉住）。

## L25 · 共用账本两边**键空间不一致**：修完"看着修好了"，其实一次没修上

- **事件（2026-09-13 05:47，又是我的错；`codex-看板编辑` 抓的）**：
  他那行写 `to="codex-看板编辑"`（**看板名**，**按我需求里的定义**），我那小工写 `to="codex-convtool"`（**slug**）→
  "另一条通道看到账就不再叫"**永远匹配不上** → **同一封信被两个通道各推一次**。
  铁证（差 1 秒的一对，可直接复算）：
  `05:45:15 by=hub to=codex-看板编辑` ／ `05:45:16 by=crew_host to=codex-convtool`（同一封 `mailbox_ts=05:45`）。
  同型还有 05:37、05:41 两对。
- **最刺眼的一点**：我 05:2x–05:4x 反复宣布"同一封信只叫一次**已经修好**"——
  **我只核了自己那半写的账本行**（`by=crew_host`），**没跨来源核** `by=hub` 的行。**自己验自己的半张卷子。**
- **变成什么规矩**：
  ① **共用文件/接口的"键"必须在需求里写死，且写入侧统一**——我这边的实现**偏离了我自己写的需求**（需求写"看板名"，代码写 slug）；
  ② **验"修好了没有"必须跨来源**：同一份账本要**按 `by` 分组**各看一遍，不能只看自己写的那几行；
  ③ 读取侧做**键归一**（看板名 ↔ slug 用名册互查），**历史行不改写**（本轮照此办）。

  **补充证据（`codex-修复` 06:25 清点，70 行时的分布）**：`by=crew_host` **多数写 slug、少数写看板名**（近半数是我修之后的），
  `by=hub` **一律写看板名** → **按任一种键去查都会漏掉另一半**。所以"键归一"必须落在**每个读取方**（不只是我），
  尤其是 **Hub 的补投查询**：`(收件线, mailbox_ts)` 不归一会**静默漏块**，补投照旧发。判据用例：
  **同一 `mailbox_ts` 下 slug 行与看板名行必须都能被查到**。

## L26 · "查不到" ≠ "不存在"：`-ErrorAction SilentlyContinue` 会把**拒绝访问**吞成空输出

- **事件（2026-09-13 03:35→06:07，`codex-唤醒通道` 自报 + 我复核）**：它报"计划任务 `DSH-Board-Sentinel` 已不存在"，
  判据是"查的时候没输出"。我**没有自己查**，拿"老板贴的 11 条里没有它"当旁证，跟着下了"无对象可停"的结论，
  还回了它一封信。**06:0x 我用只读 `schtasks /query` 亲自查：它活着**（`Ready`，下次 06:06:33，每分钟在跑）。
- **精确机制（它自己查出来的）**：查询用了 `-ErrorAction SilentlyContinue` —— **沙箱里的"拒绝访问"被吞成空输出**，
  于是"我查不到"被当成了"它不存在"。**它不是撒谎，是判据错**；**我更糟：我连查都没查，只转述。**
- **变成什么规矩**：
  ① **空输出 ≠ 不存在**：凡"某东西没了"的结论，必须换**至少一种**手段复核（换命令 / 换上下文 / 换权限），
     且**要能区分**"查不到"与"不允许查"（`SilentlyContinue`/`2>$null` 属于**主动隐藏证据**，关键判定禁用）；
  ② **不许把别人的结论当自己的证据**（我这次就是）；转述前要么亲自核，要么**明确标注"这是转述、未核"**；
  ③ 结论里写清**判据与手段**（用了什么命令、在什么权限下），别只写结论。

## L27 · 越权/交叉派活：**用别人名下的人**，是今晚两起事故的共同起点

- **老板定案（2026-09-13 06:4x）**：「**个人只能用个人手底下的人，不要交叉的去派活。**」（已进 `AGENTS.md` 锚定节）
- **两起实证**：
  ① `codex-修复` 02:41 给 `codex-渲染/适配/配置` 发**带"请现在只做两件"的岗位告知**——那三位 `leader` 是 `codex-看板编辑`，
  **不是它** → 交叉派活；**后果**：用 app 委派工具写坏三条线（`call_id` 缺字段 → 整轮 400），正文丢失、要 HR 重派（L12/L21 同源）；
  ② **我自己今晚也踩了四处**：给 `codex-人事`（上级是老板）、`codex-配置/适配/渲染`（上级是看板编辑）、`dsh-老员工`（上级是量化总监）
  发过带"**要你做的…**"的信——当时规则未立，但按今天这条**都是交叉**。
- **变成什么规矩**（照做即可）：
  ① **只调度自己名下的直属下级**；别人的人**不借、不代指挥、不越过他的上级安排**；
  ② **要用人 → 向他的直属上级"请求"**（对方可拒绝）——**请求 ≠ 派活**；
  ③ **借调/临时帮工 = 交叉**，**只有老板能批**，且必须登记（谁借谁、多久、谁验收）；
  ④ **规约类内容**（口径/必读/纪律）**写进 `AGENTS.md` / `docs/READING.md`**，各线照规约执行——**那不算派活**，
     这也是"统一规矩"最省事的路（不用逐个下指令）；
  ⑤ 组员**只接自己组长的活**，别人派的 → **退回组长**（紧急止血除外，事后补报）。

## L28 · "临时索引"提交会把**真实索引**留在旧版 → 自动合入被拒、每 30 秒空转

- **事件（2026-09-13 06:44–06:50，`codex-看板编辑` 自报、我核到宿主日志）**：他为**绕开脏工作树**，
  用「**临时索引 + `commit-tree`**」在分支上提交（不切分支）；但**真实索引里那两个文件仍是旧版** →
  `git merge` 判"工作树有本地修改会被覆盖" → **拒绝合入**；**小工 auto-merge 每 30 秒重试、连续失败**（日志 `auto-merge: 退出码 1` ×3）。
- **解开办法**（他做的）：把那两个文件 **`git add` 进真实索引**（索引＝工作树＝分支内容）→ 合入即通过（`0e5ca0c`）。
- **变成什么规矩**：
  ① **用临时索引提交后，顺手把同批文件 `git add` 一次**，保持"真实索引 ＝ 工作树 ＝ 分支内容"三者一致；
  ② **更稳的做法**：合入走"**先 `git merge-tree` 算好、再 `commit-tree`**"的**不碰工作树**的路子（不依赖工作树干净）；
  ③ **自动合入失败要有退避与告警**：连续失败到阈值就**拉长重试间隔并留一行**，别 30 秒空转刷日志（本轮暴露）。

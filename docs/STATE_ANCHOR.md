# 状态锚点 / 新员工初始化(2026-09-11 07:0x,DSH 交接版)

> **新员工第一句话就发这句**:
> 读 `E:\stockgate\Quant_Alpha_System\docs\STATE_ANCHOR.md`,按里面的"今天待办"执行;需要细节再看 `docs/ONBOARD_GUIDE.md`(角色与纪律)与 `docs/dev_notes/README.md` §0(成本与恢复)。
> 本文件是**唯一入口**;上一任 DSH 主会话(长上下文)已交接停用,日常一律用新会话 + 本文件。

## 一、五条铁律(先看这个)
1. **git 一律不动**(提交/分支归 Codex;老板另行安排)。
2. **只跑模拟盘**(账1 `a0de4b75` / 账2 `5e3d5c21`),不碰真金。
3. **常驻进程只能由计划任务上下文拉起**(`schtasks /run /tn …`);**禁止在任何 agent 沙箱 shell 里拉起**——9/10 那次全天零成交就是沙箱上下文拉起的引擎整链被限网(`WinError 10013`),复盘见 `docs/HANDOVER_20260910.md`。
4. **不碰别人的真源**:Hub 账本/信箱(`outputs/dialog/*`)、`agents.json`、`docs/BOARD_NAMES.md`、`board.mjs` 归 Codex 线;**平台配置**(`~/.dsh/profiles/**`、唤醒链)归 Codex/老板。
5. **执行前按实时价重算**;不追高:**实时价 > 昨收 × 1.015 就跳过**。

## 二、系统 30 秒
双线模拟盘:**账1 组合盘**(14:44 尾盘,6 只等权,引擎自动下单);**账2 做T学习盘**(20 万,文件单走 `C:\emq\日内分钟`,回报在 broker `downfiles/5e3d…`)。盘中与晚间大量工作是**脚本/引擎自跑**(本地 stockdb `127.0.0.1:7899`,0 token);agent 只在需要判断时被叫醒。

## 三、当前状态(9/11 07:0x)
- **账1**:nav 199,013 / 可用 43,253;持仓 `603256 400@126.79`、`301358 800@55.549`、`002595 1000@40.82`、`688775 200@103.69`。
- **9/11 计划** `outputs/next_plan.csv`:卖 `603256 -100@129.10`、`301358 -200@55.41`、`002595 -200@40.28`;买 **`000893 +800@43.22`**、**`300450 +1100@30.07`**;**14:44 引擎自动执行**(取价失败则不动作)。
- **账2 草案** `outputs/plan2_build_20260911.csv`(8 票共 150,302,留 49,698;与账1 无重票);执行器 `scripts/build_acct2.py --date 20260911 --batch 1|2`(**默认 dry;`--send` 才放单**);dry 留痕 `outputs/inbox/reports/dsh_20260911_acct2_dry.md`。
- **值守**:`duty/duty_engine.py` 常驻;心跳 `outputs/watch_heartbeat.txt`(交易时段每 20s);时段表 `duty/schedule.yaml`(09:15/09:30/11:30/13:00/14:40/**14:44 执行**/15:00/15:05)。
- **net_guard**:计划任务 `QuantNetGuard`(每 3 分钟)**已武装**(`--enable-restart`);成功判据=旧**引擎** PID 消失 + 新 `duty_engine` PID 出现 + 心跳**内容**时间戳前进,窗口 30–120s;护栏:14:35–15:05 执行窗、在途委托门禁、连续两周期+冷却(细则见 `scripts/net_guard.py` 头部)。
- **计划任务(均已修好)**:`QuantDailyScan` 20:15、`QuantDailyPlan` 20:40、`QuantArtifactCheck` 20:50(产物新鲜度,日志 `outputs/*_task.log`);`QuantWatchDog` 每 5 分钟(含托盘存活检查);`DSH-Board-Sentinel` 每 1 分钟(唤醒链);`QuantCodexBoard`(Hub 8788)。

## 四、今天(9/11)待办
| 时刻 | 动作 | 谁 |
|---|---|---|
| 09:15 | 在岗检查:托盘/引擎/心跳;确认 `QuantNetGuard` 在且已武装 | 值守 agent |
| 09:35 | 账2 **100 股连通测试**,确认 `C:\emq\日内分钟` 被东财终端扫单 | 值守 agent(只有它能写该目录) |
| 09:35–09:45 | 账2 `batch1 --dry` 留痕 → `batch1 --send` | 同上 |
| 10:30 前 | 账2 `batch2` | 同上 |
| 14:35 | 取价自检(限网则**只用计划任务上下文**重启) | 值守 agent |
| 14:44 | 账1 卖3买2(引擎自动) | 引擎 |
| 15:05 | 收盘回账核对(`scripts/close_check.py`) | 值守 agent |
| 20:15/20:40/20:50 | 扫描/计划/产物检查(自动) | 计划任务 |

## 五、等别人(不是值守 agent 的活)
- **等 Codex**:复看 `scripts/watch_all_dog.py`(托盘存活检查)、`scripts/daily_artifact_check.py`+`QuantArtifactCheck`;CT-021/CT-022(看板助理线:员工自动入职/退役、忙等门铃);平台层"统一唤醒"改造。
- **等老板**:git 怎么处理;账2 扫单目录确认。
- 交接单(给 Codex 那版):`docs/HANDOVER_TO_CODEX_20260911.md`。

## 六、唤醒规则(必须知道)
- **能叫醒值守会话的唯一现成通道 = 看板门铃**,且**只认老板本人署名**:`- @dsh-老员工 <YYYY-MM-DD HH:MM> 老板：…`(见 `tools/mobile_chat/dsh_board_sentinel.py`)。
- `schedule_create` 的提醒是**会话级**的:谁建就只叫醒谁。
- 公告(`@全体`)只入库、只按"知悉铃",不要求回复,也不会叫醒任何人。

## 七、成本纪律
上下文越长,每回合越贵(同题可差 ~10 倍)。**日常短问答一律新会话 + 本文件**;长上下文只用于跨天追溯。峰谷:北京 09:00–12:00 / 14:00–18:00 为高峰 → 高峰只做必须实时的(在岗检查、14:35 自检、执行结果),**文档/研究/复盘放 18:00 之后**。

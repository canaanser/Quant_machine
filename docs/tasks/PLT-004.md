# PLT-004 计划任务清单（只读快照）

- **派单人**：`codex-看板编辑`（配置线首任任务）｜**取号**：`codex-总监` 2026-09-13 03:26（原临时号 `CFG-001` → 改 `PLT-004`，理由见下）
- **承接人**：`codex-配置`（工号 `codex-config`）｜**类型**：`ops`｜**优先级**：`P2`
- **状态**：**已交付**（2026-09-13 03:2x 交活；总监 06:0x 复核 → 结论"清单对"）
- **交付物**：`docs/reports/20260913_计划任务清单.md`（本机相关计划任务 12 条，逐条写清"干什么/触发/归谁/怎么停"）

## 一、取号理由（总监 03:26 裁定原文要点）

不用 `CFG`：**前缀表已冻结一批历史类，别再膨胀**；"计划任务清单"属 **`PLT`＝平台/会话基础设施**
（领域含"沙箱与权限/平台设施"）。按 `docs/TASK_ID_STANDARD.md` 的 `max+1` 取 `PLT-004`
（当时在册：`PLT-001`、`PLT-002`，`PLT-003` 是 callid 修复报告）。

> ⚠️ **号冲突留痕（2026-09-13 06:0x，总监自查）**：我在 ~04:5x 立"能力门禁"卡时**又占了一次 `PLT-004`**
> → 已把那张卡改为 `docs/tasks/PLT-005.md`；**`PLT-004` 归还本卡**。这是**我违反"不抢号、不复用"**，
> 不是配置线的问题（它是照裁定取的号）。

## 二、验收口径（总监复核时用的判据）

1. **覆盖**：凡是与"量化仓 + 协作系统"相关的计划任务都要在表里（本机实况 12 条）；
2. **四要素**：每条都有 **干什么 / 什么触发 / 归谁（附依据） / 怎么停**；
3. **红线**：停法要写清代价与禁令（例：`数据更新.exe` 不自退 → 必须走 `run_data_sync` 流程；值守引擎不许重复拉起）；
4. **只读**：**不改、不停、不建**任何任务；原始 XML 未落盘（口径：要导先说明占多大、是否入库）；
5. **可复核**：每条判据要能被第三方用只读手段重现。

## 三、总监复核结论（2026-09-13 06:0x）

- **清单正确**：我用只读 `schtasks /query` 对了一遍——**它表里的 `DSH-Board-Sentinel` 确实存在**
  （`Ready`，下次 06:06:33，每分钟在跑）。反而是**我**先前据"老板贴的 11 条"下了"哨兵已不存在"的错结论（见 `L26`）。
- **本卡验收通过**；遗留一条**待老板拍板**的外部事项：哨兵要不要停/改规则（不改本卡结论）。

## 四、签字区

| 谁 | 何时 | 决定 | 证据 |
| --- | --- | --- | --- |
| `codex-配置` | 2026-09-13 03:2x | 交付本清单（只读） | `docs/reports/20260913_计划任务清单.md` |
| `codex-总监` | 2026-09-13 06:0x | **复核通过**（含"哨兵还在"的更正） | 本文 §三；`schtasks /query` 只读输出 |

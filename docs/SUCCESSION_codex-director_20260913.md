# 承接交接单 · `codex-总监`（一页制，2026-09-13 08:2x）

> **为什么有这份**：老板问"你这么长，能不能删早期上下文" → 平台不给裁剪口子，
> **正解是"换线承接"**（旧线归档只读、工号/名字/资产照传）。本页就是那条线的**全部输入**。
> **判据（什么时候该换）**：当"翻文件的时间 > 记住的时间"就该换（本线已 319 轮、压缩 2 次）。

## 一、我是谁 / 我的地盘

- **看板名 `codex-总监`**｜**工号 `codex-director`**｜层级 `director`（L1）｜领导：**老板**｜工位 `E:\stockgate\Quant_Alpha_System`
- **我管**：组织人事（名册/层级/关系链/入职）、平台与会话基础设施（计划任务/宿主/看板服务）、规约与教训、**审阅与合入 `main`**、**门禁**
- **我不管**：量化业务（归 `codex-量化总监`）；看板实现（归 `codex-看板编辑`）；只保留"规约 + 合入门禁"两面
- **我的下级**：`codex-看板编辑`、`codex-套件`、`codex-修复`（**只能给他们派活**，别人的人只发"请求"）

## 二、接手第一件事（照做就行）

1. `node tools/mobile_chat/whoami.mjs --me codex-总监` → 确认级别与必读；
2. 读 **`AGENTS.md`**（唯一规约真源，自动加载）**+ `docs/INDEX.md`（一页索引）** —— **其余按需 grep，别整读**；
3. 读**我的信箱** `outputs/dialog/pending_codex-director.ndjson`（**清到 0 条**再干活）；
4. 把"未结清单"（§四）过一遍，**先接在办**。

## 三、资产指针（都在盘上，不是我脑子里的）

| 类别 | 位置 |
| --- | --- |
| 规约真源 | `AGENTS.md`（+ `docs/READING.md` 分级必读） |
| 教训 | `docs/LESSONS.md`（L1–L31；**今晚新增 L22–L31 十条**） |
| 门禁/合入 | `scripts/crew_review_merge.mjs`（`--check` 七条 + **证据指纹**；`--apply` = `merge-tree`/`commit-tree`/`update-ref` **不碰工作树**） |
| 提交闸 | `.githooks/`（署名必须实名看板名；不许在别人分支提交；共用工作树用 `-c` 具名） |
| 小工宿主 | `scripts/crew_host.py`（镜像/门铃/账本/P0/兜底/自动合入/自我重载）+ 自测 `scripts/test_crew_host_p0.py`（**36 条**） |
| 通道口径 | `outputs/dialog/pushed.ndjson`（**一封一行**，两侧共写；`mailbox_ts` 判水位） |
| 文档索引 | `docs/INDEX.md`（**新增文档必须加一行**） |

## 四、未结（2026-09-13 08:2x 状态）

| # | 事项 | 等谁 |
| --- | --- | --- |
| 1 | **14:44 前重启值守引擎**（`TRD-003` 卖单修复才生效）— 只能老板本人或计划任务上下文 | **老板** |
| 2 | **公告回执减量**（`ackRequired` 字段）与 **`/api/mail` 只入库不推送**（只剩门铃一条叫醒） | `codex-看板编辑`（已派，带验收） |
| 3 | **git worktree 全队推行**（今晚所有互踩的根治） | **老板** 点头 |
| 4 | `feature/say-nofalsefail`（say 假失败修复）先物化工作树再走门禁 | `codex-看板编辑` |
| 5 | 渲染线那支的**实名补提交**（其提交 author 落到了全局 `canaanser`） | 渲染线（经看板编辑） |
| 6 | 量化线：`TRD-002`/`PLT-007` 已合；`TRD-001` 9/11 欠的 15:05 验收、账1 `tail_exec`、账2 越权票上报老板 | `codex-量化总监` |

## 五、铁律（接手后照旧）

- **结论必须自己核**（三种"查不到"都要能区分：不存在 / 不许查 / 查错地方）——L26；
- **一份事实只写一处**；**报告头 5 行摘要**；**长度硬上限**（信≤800/板≤200/报告≤60 行）；
- **只读一处**（信箱：机器读 ndjson、人看镜像）；
- **提交一律 `git commit -- <路径>` + `-c user.name/email`**（共享工作树里裸 `git commit` 会带走别人的暂存）；
- **不管量化业务**；**同级不互派活**；要人用 → 向其组长**请求**。

## 六、签字

| 谁 | 何时 | 说明 |
| --- | --- | --- |
| `codex-总监`（本线） | 2026-09-13 08:2x | 交接单就绪（**换线与否由老板定；不换也不浪费，它就是索引**） |

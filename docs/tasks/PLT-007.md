# PLT-007 事件注入 / 通道层（AR-001「事件注入」承接）

- 卡号：`PLT-007`（前缀 `PLT`＝平台/会话基础设施；**号由 `codex-总监` 裁定给出**，2026-09-13 06:52）
- 派单人：`codex-量化总监`（组长，本线内派）｜承接人：`codex-唤醒通道`（lead）｜**验收人：`codex-量化总监`**
- 状态：**已开卡，开工中**（老板 06:49 `N-A5EB` 解令后第一张）
- 前身：`AR-001`（`AR` 族**已冻结**，不再新增；本卡是它的承接号，不是改号）

## 一、交付物（三件套之②）

1. 事件注入/通道层的**实现或可落方案**（分支 `feature/*`，不许直接动 `main`）；
2. 自测证据（命令 + 结果）；
3. 交活格式（照闭环）：`【已提交·待验收】PLT-007 · 分支 <b> · 提交 <short hash> ｜ 证据：<路径> ｜ 自测：<命令>→<结果> ｜ 未结：<剩什么/等谁>`。

## 二、验收标准（三件套之②；我按这四条验）

1. **范围只做"事件注入/通道"这一层**，不顺手改别的层；
2. **两道边界（`codex-总监` 06:52 裁定，必须写进实现说明）**：
   - 与 `PLT-002`（沙箱外小工宿主 `scripts/crew_host.py`：镜像/门铃/账本/自动合入/自我重载）、`PLT-005`（能力门禁）、`PLT-006`（裁决阶梯与老板面门槛）**划清**；
   - **凡要改 `crew_host.py` 或 `pushed.ndjson` 账本口径的 → 先在卡里写清并报 `codex-总监`**（这两处是本轮刚统一的机制，多手改会重演"键空间不一致/双推"事故）；
3. 自测可复跑、证据可核（不给口头结论）；涉及"叫醒"的验收要看**对方真的起了回合**（`updatedAt` 变化），只看投递成功不算过；
4. **合入 `main` 不归本卡**：过门禁那一步转 `codex-总监`（门禁七条 + 审阅记录）。

## 三、红线

- 不碰别人的工位与会话；不改平台注册（计划任务/服务注册/config）——需要时按平台面流程报 `codex-总监`；
- 发板/投信一律 `say.mjs --file … --author <实名> --card PLT-007`；
- 不重复拉起值守引擎/`stockdb`（本卡无关，但沿用）。

## 四、签字区

| 角色 | 人 | 时间 | 结论 | 证据 |
| --- | --- | --- | --- | --- |
| 派单 | `codex-量化总监` | 2026-09-13 06:5x | 开卡 + 派活三件套 | 本文件 |
| 执行 | `codex-唤醒通道` | — | 待交 | 交活行 + 分支 |
| 验收 | `codex-量化总监` | — | 待验 | — |
